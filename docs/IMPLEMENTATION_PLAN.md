# 🚀 План реализации: Интеграция Frontend + Backend + Доступ для ребёнка

## 📋 Этапы работы

### Этап 1: Регистрация и авторизация родителя ✅

**Что нужно:**
- [x] Backend API для регистрации (нужно добавить)
- [ ] Frontend форма регистрации
- [ ] Frontend форма входа
- [ ] Сохранение JWT токена

**Эндпоинты:**
```python
POST /api/auth/register  # Регистрация родителя
POST /api/auth/login      # Вход (уже есть)
POST /api/auth/logout     # Выход (уже есть)
```

---

### Этап 2: Создание профиля ребёнка ✅

**Что нужно:**
- [x] Backend API для создания ребёнка (уже есть)
- [ ] Frontend форма создания ребёнка
- [ ] Загрузка аватара
- [ ] Настройка имени и пола

**Эндпоинты:**
```python
POST /api/children/       # Создание ребёнка (уже есть)
PUT /api/children/{id}    # Обновление (уже есть)
GET /api/children/        # Список детей (уже есть)
```

---

### Этап 3: Генерация доступа для ребёнка ⭐

**Вариант A: QR-код + PIN (рекомендуется)**

**Что нужно:**
- [ ] Backend: Генерация QR-кода с токеном
- [ ] Backend: Установка PIN-кода
- [ ] Backend: Вход по QR/PIN
- [ ] Frontend: Отображение QR-кода для родителя
- [ ] Frontend: Сканер QR-кода для ребёнка
- [ ] Frontend: Форма ввода PIN

**Эндпоинты:**
```python
POST /api/children/{child_id}/generate-access
# Возвращает: { "qr_code": "...", "pin": "1234", "qr_token": "..." }

POST /api/auth/child-qr
# { "qr_token": "..." } → возвращает JWT токен

POST /api/auth/child-pin
# { "child_id": 1, "pin": "1234" } → возвращает JWT токен

PUT /api/children/{child_id}/pin
# { "pin": "5678" } → изменение PIN
```

**Вариант B: Только PIN (проще)**

**Что нужно:**
- [ ] Backend: Установка PIN при создании ребёнка
- [ ] Backend: Вход по PIN
- [ ] Frontend: Форма ввода PIN для ребёнка

---

### Этап 4: Интеграция Frontend с Backend 🔄

**Что нужно заменить:**

1. **data.js** - заменить localStorage на API:
   ```javascript
   // Было:
   localStorage.getItem('responsibilityAppData')
   
   // Станет:
   await apiClient.getTasks()
   await apiClient.getStars()
   await apiClient.getPiggy()
   ```

2. **app.js** - добавить авторизацию:
   ```javascript
   // При загрузке
   const token = await checkAuth()
   if (!token) {
     showLoginScreen()
   } else {
     loadDataFromAPI()
   }
   ```

3. **ui.js** - обновить сохранение:
   ```javascript
   // Было:
   saveData() // localStorage
   
   // Станет:
   await apiClient.createTask(task)
   await apiClient.addStars(description, stars)
   ```

---

### Этап 5: Разделение интерфейсов 👨‍👩‍👧

**Интерфейс родителя:**
- Регистрация/вход
- Создание профиля ребёнка
- Настройки (курс обмена, цели)
- Просмотр статистики
- Управление задачами
- Админка

**Интерфейс ребёнка:**
- Вход по QR/PIN
- Просмотр задач
- Отметка выполненных задач
- Просмотр звёзд и копилки
- Дневник
- Список желаний
- БЕЗ настроек и админки

---

## 🎯 Рекомендуемый порядок реализации

### Приоритет 1: Базовая интеграция
1. ✅ Добавить регистрацию родителя
2. ✅ Интегрировать frontend с backend для задач
3. ✅ Интегрировать frontend с backend для звёзд
4. ✅ Интегрировать frontend с backend для копилки

### Приоритет 2: Доступ для ребёнка
5. ✅ Реализовать PIN-код для ребёнка (проще чем QR)
6. ✅ Создать упрощённый интерфейс для ребёнка
7. ✅ Разделить интерфейсы (родитель/ребёнок)

### Приоритет 3: Улучшения
8. ✅ Добавить QR-код (опционально)
9. ✅ Добавить синхронизацию в реальном времени
10. ✅ Добавить офлайн режим с синхронизацией

---

## 📝 Примеры кода

### Регистрация родителя:
```python
# backend/routers/auth.py
@router.post("/register")
async def register(request: RegisterRequest, db: AsyncSession = Depends(get_db)):
    auth_service = AuthService(db)
    user = await auth_service.register(
        email=request.email,
        password=request.password,
        role=UserRole.PARENT
    )
    return {"user_id": user.id, "message": "Регистрация успешна"}
```

### Вход ребёнка по PIN:
```python
# backend/routers/auth.py
@router.post("/child-pin")
async def child_pin_login(request: ChildPinRequest, db: AsyncSession = Depends(get_db)):
    # Проверка PIN
    child = await get_child_by_pin(request.child_id, request.pin)
    if not child:
        raise HTTPException(401, "Неверный PIN")
    
    # Создание токена для ребёнка
    token = create_child_token(child.id)
    return {"access_token": token, "child": child}
```

### Frontend: Загрузка данных с сервера
```javascript
// frontend/src/js/data.js
async function loadDataFromAPI() {
  try {
    const tasks = await apiClient.getTasks()
    const stars = await apiClient.getStars()
    const piggy = await apiClient.getPiggy()
    
    // Обновляем appData
    appData.checklist = tasks.checklist
    appData.kanban = tasks.kanban
    appData.stars = stars
    appData.piggy = piggy
    
    // Рендерим UI
    renderChecklist()
    renderKanban()
    updateStars()
    renderPiggy()
  } catch (error) {
    console.error('Ошибка загрузки данных:', error)
  }
}
```

---

## 🔐 Безопасность

### Для доступа ребёнка:
- ✅ PIN хранится в зашифрованном виде (bcrypt)
- ✅ Ограничение попыток входа (5 попыток, затем блокировка на 15 минут)
- ✅ JWT токен с коротким временем жизни (15 минут)
- ✅ Refresh token в HttpOnly cookie
- ✅ Проверка согласия родителей перед действиями

### Для данных:
- ✅ Все данные в базе данных (не в localStorage)
- ✅ Резервное копирование
- ✅ Логирование всех действий
- ✅ Аудит изменений

---

## 📱 Интерфейсы

### Интерфейс родителя:
```
┌─────────────────────────┐
│  Регистрация/Вход       │
│  ↓                      │
│  Создание профиля       │
│  ↓                      │
│  Настройки              │
│  - Курс обмена          │
│  - Задачи               │
│  - Цели                 │
│  ↓                      │
│  Генерация доступа      │
│  - QR-код               │
│  - PIN-код              │
│  ↓                      │
│  Просмотр статистики    │
└─────────────────────────┘
```

### Интерфейс ребёнка:
```
┌─────────────────────────┐
│  Вход (QR/PIN)          │
│  ↓                      │
│  Главный экран          │
│  - Задачи               │
│  - Звёзды               │
│  - Копилка              │
│  ↓                      │
│  Дневник                │
│  Список желаний         │
└─────────────────────────┘
```

---

## ✅ Чеклист реализации

### Backend:
- [ ] Добавить регистрацию (`/api/auth/register`)
- [ ] Добавить генерацию доступа для ребёнка
- [ ] Добавить вход по PIN (`/api/auth/child-pin`)
- [ ] Добавить вход по QR (`/api/auth/child-qr`) - опционально
- [ ] Добавить проверку роли в dependencies

### Frontend:
- [ ] Добавить форму регистрации
- [ ] Добавить форму входа
- [ ] Интегрировать data.js с API
- [ ] Заменить localStorage на API вызовы
- [ ] Добавить форму входа для ребёнка (PIN)
- [ ] Создать упрощённый интерфейс для ребёнка
- [ ] Разделить интерфейсы (родитель/ребёнок)

### Тестирование:
- [ ] Регистрация родителя
- [ ] Создание профиля ребёнка
- [ ] Вход ребёнка по PIN
- [ ] Синхронизация данных между устройствами
- [ ] Проверка безопасности

