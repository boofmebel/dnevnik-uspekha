# 📋 Где взять данные сервера для деплоя

## 🎯 Вариант 1: REG.RU (российский хостинг)

### Шаг 1: Войдите в панель управления REG.RU

1. Откройте: https://www.reg.ru/user/
2. Войдите в личный кабинет

### Шаг 2: Найдите данные вашего сервера

#### Если у вас VPS/выделенный сервер:

1. Перейдите в раздел **"VPS"** или **"Выделенные серверы"**
2. Выберите ваш сервер
3. Найдите информацию:
   - **IP-адрес сервера** (например: `123.45.67.89`)
   - **Root-пароль** или данные для SSH доступа
   - **Порт SSH** (обычно `22`)

#### Если у вас виртуальный хостинг:

⚠️ **Внимание:** Виртуальный хостинг обычно не поддерживает SSH доступ для деплоя. 
Вам нужен **VPS** (виртуальный выделенный сервер) или **выделенный сервер**.

### Шаг 3: Создайте пользователя для деплоя (рекомендуется)

Не используйте `root` напрямую! Создайте отдельного пользователя:

```bash
# Подключитесь к серверу как root
ssh root@ваш-ip-адрес

# Создайте пользователя для деплоя
adduser deploy
usermod -aG sudo deploy

# Настройте SSH доступ для нового пользователя
mkdir -p /home/deploy/.ssh
chmod 700 /home/deploy/.ssh
# (скопируйте ваш публичный SSH ключ сюда)
chown -R deploy:deploy /home/deploy/.ssh
```

### Шаг 4: Определите путь для проекта

Обычно проекты размещают в:
- `/var/www/название-проекта` (для веб-серверов)
- `/home/deploy/название-проекта` (для пользовательских проектов)
- `/opt/название-проекта` (для приложений)

**Рекомендуемый путь:** `/var/www/dnevnik-uspekha`

---

## 🎯 Вариант 2: Другие хостинг-провайдеры

### DigitalOcean, AWS, Google Cloud, Yandex Cloud

1. Войдите в панель управления провайдера
2. Найдите раздел **"Servers"**, **"Instances"** или **"Compute"**
3. Выберите ваш сервер
4. Найдите:
   - **Public IP** (публичный IP-адрес)
   - **SSH ключ** или пароль для доступа
   - **Пользователь** (обычно `root`, `ubuntu`, `admin`)

---

## 📝 Сводка данных, которые нужны

Для настройки деплоя вам понадобятся:

| Параметр | Пример | Где найти |
|----------|--------|-----------|
| **SERVER_HOST** | `123.45.67.89` или `server.example.com` | IP-адрес или домен сервера в панели хостинга |
| **SERVER_USER** | `deploy` или `root` | Пользователь для SSH (создайте отдельного пользователя) |
| **SERVER_PATH** | `/var/www/dnevnik-uspekha` | Путь, где будет размещён проект |
| **SERVER_PORT** | `22` | SSH порт (обычно 22, может быть другой) |

---

## 🔧 Пошаговая подготовка сервера

### 1. Подключитесь к серверу

```bash
ssh root@ваш-ip-адрес
# или
ssh deploy@ваш-ip-адрес
```

### 2. Установите необходимые компоненты

```bash
# Обновление системы (Ubuntu/Debian)
apt update && apt upgrade -y

# Установка Git
apt install git -y

# Установка Python и pip (для backend)
apt install python3 python3-pip python3-venv -y

# Установка PostgreSQL (если нужна БД)
apt install postgresql postgresql-contrib -y

# Установка Nginx (для веб-сервера)
apt install nginx -y
```

### 3. Создайте директорию для проекта

```bash
# Создайте директорию
mkdir -p /var/www/dnevnik-uspekha
cd /var/www/dnevnik-uspekha

# Установите права доступа
chown -R deploy:deploy /var/www/dnevnik-uspekha
chmod -R 755 /var/www/dnevnik-uspekha
```

### 4. Клонируйте репозиторий (вручную, первый раз)

```bash
cd /var/www/dnevnik-uspekha
git clone https://github.com/boofmebel/dnevnik-uspekha.git .
```

---

## 🚀 Если сервера ещё нет

### REG.RU - Заказ VPS

1. Перейдите: https://www.reg.ru/vps/
2. Выберите тариф (минимум 1GB RAM для начала)
3. Выберите ОС (Ubuntu 22.04 LTS рекомендуется)
4. Закажите сервер
5. Дождитесь активации (обычно 5-15 минут)
6. Получите данные доступа на email

### Альтернативные провайдеры

- **Yandex Cloud**: https://cloud.yandex.ru/
- **Timeweb**: https://timeweb.com/
- **DigitalOcean**: https://www.digitalocean.com/
- **AWS**: https://aws.amazon.com/

---

## ✅ Проверка готовности сервера

Перед настройкой деплоя убедитесь:

```bash
# 1. Проверьте SSH доступ
ssh ваш-пользователь@ваш-ip-адрес

# 2. Проверьте наличие Git
git --version

# 3. Проверьте Python
python3 --version

# 4. Проверьте права на директорию
ls -la /var/www/dnevnik-uspekha
```

---

## 📞 Что делать, если не знаете данные

### В REG.RU:

1. **Техподдержка**: https://www.reg.ru/support/
2. **Чат поддержки**: в личном кабинете
3. **Телефон**: 8 800 200-70-70 (бесплатно по России)

Спросите:
- "Какой IP-адрес у моего VPS сервера?"
- "Как подключиться по SSH?"
- "Какой пароль root?"

### Если сервера нет:

1. Закажите VPS в REG.RU (от 200₽/месяц)
2. Или используйте бесплатный вариант (Heroku, Railway, Render) - но там другой процесс деплоя

---

## 🎯 Быстрый чеклист

- [ ] Есть ли у вас сервер? (VPS/выделенный)
- [ ] Знаете ли вы IP-адрес сервера?
- [ ] Есть ли SSH доступ?
- [ ] Создан ли пользователь для деплоя?
- [ ] Определён ли путь для проекта?
- [ ] Установлены ли Git, Python на сервере?

Если все пункты отмечены - можно запускать скрипт настройки деплоя! 🚀

