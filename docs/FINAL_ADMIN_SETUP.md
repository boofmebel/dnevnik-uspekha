# ✅ Администратор готов к созданию!

## 📋 Что сделано

1. ✅ Создан SQL скрипт: `scripts/create_admin.sql`
2. ✅ Скрипт скопирован на сервер: `/tmp/create_admin.sql`
3. ✅ Хеш пароля сгенерирован

## 🔑 Данные для входа

- **Email:** `79059510009@mail.ru`
- **Пароль:** `Admin123!`

---

## 🚀 Выполнение SQL на сервере

**Вариант 1: Если есть доступ к PostgreSQL на сервере**

```bash
ssh root@89.104.74.123
psql -U postgres -d dnevnik_uspekha -f /tmp/create_admin.sql
```

**Вариант 2: Если база данных в другом месте**

Найдите способ подключиться к базе данных и выполните SQL из файла `/tmp/create_admin.sql` на сервере.

**Вариант 3: Через Python скрипт (если есть окружение на сервере)**

```bash
ssh root@89.104.74.123
cd /var/www/dnevnik-uspekha
python3 scripts/create_admin_simple_db.py 79059510009@mail.ru Admin123!
```

---

## ✅ После выполнения SQL

1. Откройте: `http://89.104.74.123:3000`
2. Войдите с:
   - Email: `79059510009@mail.ru`
   - Пароль: `Admin123!`
3. Откройте админку: `http://89.104.74.123:3000/admin.html`

---

## 📝 SQL команда (для копирования)

```sql
INSERT INTO users (email, password_hash, role, created_at)
VALUES ('79059510009@mail.ru', '$2b$12$...', 'admin', NOW())
ON CONFLICT (email) DO UPDATE
SET role = 'admin', password_hash = EXCLUDED.password_hash, updated_at = NOW();
```

Полный SQL находится в файле `/tmp/create_admin.sql` на сервере.

---

## ❓ Если не получается подключиться к базе

База данных может быть:
- В облаке (AWS RDS, DigitalOcean, etc.)
- На другом сервере
- В Docker контейнере
- Защищена файрволом

Проверьте конфигурацию backend на сервере для получения параметров подключения.

