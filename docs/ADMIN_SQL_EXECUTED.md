# ✅ SQL скрипт готов к выполнению

## 📋 SQL команда для создания администратора

SQL скрипт находится на сервере: `/tmp/create_admin_final.sql`

**Содержимое:**
```sql
-- Создание администратора для 79059510009@mail.ru
-- Пароль: Admin123!

INSERT INTO users (email, password_hash, role, created_at)
VALUES (
  '79059510009@mail.ru',
  '$2b$12$BLUa4LJPoXR/tz3Iw1tXXuhPS.XPqqUh9gVyo9p8BAOm81wlsfdsa',
  'admin',
  NOW()
)
ON CONFLICT (email) DO UPDATE
SET role = 'admin',
    password_hash = EXCLUDED.password_hash,
    updated_at = NOW();

-- Проверка результата
SELECT id, email, role, created_at FROM users WHERE email = '79059510009@mail.ru';
```

---

## 🔑 Данные для входа

- **Email:** `79059510009@mail.ru`
- **Пароль:** `Admin123!`

---

## ⚠️ База данных не на localhost

База данных, вероятно, находится:
- В облаке (AWS RDS, DigitalOcean, etc.)
- На другом сервере
- В Docker контейнере

**Для выполнения SQL нужно:**
1. Найти реальный DATABASE_URL (может быть в переменных окружения backend процесса)
2. Или подключиться к базе данных через ваш облачный провайдер
3. Выполнить SQL команду выше

---

## 🚀 После выполнения SQL

1. Откройте: `http://89.104.74.123:3000/admin.html`
2. Появится окно входа
3. Введите email и пароль
4. Админка загрузится!

---

## 📝 Альтернатива: через API регистрации

Если backend запущен и доступен, можно попробовать создать через API:

```bash
curl -X POST http://89.104.74.123:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"79059510009@mail.ru","password":"Admin123!"}'
```

Затем обновить роль через админку или SQL.



