# 🔐 Админ-панель "Дневник успеха"

## 📋 Описание

Полноценная админ-панель для управления всем сайтом:
- Просмотр всех пользователей
- Управление детьми
- Мониторинг подписок
- Просмотр уведомлений
- Статистика сайта

---

## 🔗 Ссылка на админку

**Продакшн:**
```
http://89.104.74.123:3000/admin
```

**Тест:**
```
http://89.104.74.123:8080/admin
```

---

## 🔑 Доступ

Только пользователи с ролью `ADMIN` могут получить доступ к админ-панели.

### Как создать админа:

1. **Через базу данных:**
```sql
UPDATE users SET role = 'admin' WHERE email = 'admin@example.com';
```

2. **Через API (если есть админ):**
```bash
PUT /api/admin/users/{user_id}
{
  "role": "admin"
}
```

---

## 📊 Функционал админки

### 1. Статистика (Dashboard)

**Эндпоинт:** `GET /api/admin/stats`

**Что показывает:**
- Общее количество пользователей
- Количество родителей
- Количество детей
- Активные подписки
- Всего подписок
- Запросы на возврат
- Всего уведомлений
- Последние пользователи (10)
- Последние подписки (10)
- Последние уведомления (20)

---

### 2. Пользователи

**Эндпоинт:** `GET /api/admin/users`

**Параметры:**
- `skip` - пропустить N записей (пагинация)
- `limit` - количество записей (макс 100)
- `role` - фильтр по роли (parent, child, admin)

**Что видно:**
- Email пользователя
- Роль
- Количество детей
- Количество подписок
- Дата регистрации

**Действия:**
- `PUT /api/admin/users/{id}` - Обновить пользователя (роль, email)
- `DELETE /api/admin/users/{id}` - Удалить пользователя

---

### 3. Дети

**Эндпоинт:** `GET /api/admin/children`

**Параметры:**
- `skip` - пропустить N записей
- `limit` - количество записей

**Что видно:**
- Имя ребёнка
- Email родителя
- Пол
- Количество задач
- Всего звёзд
- Дата создания

---

### 4. Подписки

**Эндпоинт:** `GET /api/admin/subscriptions`

**Параметры:**
- `skip` - пропустить N записей
- `limit` - количество записей
- `active_only` - только активные подписки

**Что видно:**
- Email пользователя
- Дата начала
- Дата окончания
- Статус (активна/неактивна)
- Запрос на возврат
- Причина возврата

---

### 5. Уведомления

**Эндпоинт:** `GET /api/admin/notifications`

**Параметры:**
- `skip` - пропустить N записей
- `limit` - количество записей
- `type` - фильтр по типу (subscription, refund, complaint, consent, system)

**Что видно:**
- Email пользователя
- Тип уведомления
- Сообщение
- Статус (pending, sent, read, failed)
- Дата создания

---

## 🎨 Интерфейс админки

### Главная страница (Dashboard)

```
┌─────────────────────────────────────────┐
│  🔐 Админ-панель                       │
├─────────────────────────────────────────┤
│                                         │
│  📊 Статистика                         │
│  ┌─────────┬─────────┬─────────┐      │
│  │ Пользователи: 150 │         │      │
│  │ Родители: 100     │         │      │
│  │ Дети: 200         │         │      │
│  └─────────┴─────────┴─────────┘      │
│                                         │
│  💳 Подписки                           │
│  ┌─────────┬─────────┬─────────┐      │
│  │ Активных: 50      │         │      │
│  │ Всего: 80         │         │      │
│  │ Возвратов: 5      │         │      │
│  └─────────┴─────────┴─────────┘      │
│                                         │
│  📋 Последние пользователи             │
│  [Таблица с пользователями]            │
│                                         │
│  💰 Последние подписки                 │
│  [Таблица с подписками]                │
│                                         │
└─────────────────────────────────────────┘
```

### Страница пользователей

```
┌─────────────────────────────────────────┐
│  👥 Пользователи                       │
├─────────────────────────────────────────┤
│  [Фильтр по роли] [Поиск]              │
│                                         │
│  Email              │ Роль │ Дети │   │
│  ────────────────────────────────────  │
│  parent1@mail.com   │ Parent│ 2   │   │
│  parent2@mail.com   │ Parent│ 1   │   │
│  admin@mail.com     │ Admin │ 0   │   │
│                                         │
│  [◀ Предыдущая] [1] [2] [3] [Следующая▶]│
└─────────────────────────────────────────┘
```

---

## 🔒 Безопасность

### Проверка прав

Все эндпоинты админки проверяют роль пользователя:

```python
def check_admin(current_user: dict = Depends(get_current_user)) -> dict:
    """Проверка прав администратора"""
    if current_user.get("role") != UserRole.ADMIN:
        raise HTTPException(
            status_code=status.HTTP_403_FORBIDDEN,
            detail="Требуются права администратора"
        )
    return current_user
```

### Защита от удаления

- Админ не может удалить самого себя
- При удалении пользователя удаляются все связанные данные (дети, подписки и т.д.)

---

## 📝 API Эндпоинты

### Статистика
- `GET /api/admin/stats` - Общая статистика

### Пользователи
- `GET /api/admin/users` - Список пользователей
- `PUT /api/admin/users/{id}` - Обновить пользователя
- `DELETE /api/admin/users/{id}` - Удалить пользователя

### Дети
- `GET /api/admin/children` - Список всех детей

### Подписки
- `GET /api/admin/subscriptions` - Список подписок

### Уведомления
- `GET /api/admin/notifications` - Список уведомлений

---

## 🚀 Использование

### 1. Вход в админку

1. Войдите как администратор
2. Откройте: `http://89.104.74.123:3000/admin`
3. Если у вас нет прав - получите ошибку 403

### 2. Просмотр статистики

Откройте главную страницу админки - увидите общую статистику.

### 3. Управление пользователями

- Перейдите в раздел "Пользователи"
- Фильтруйте по роли
- Обновляйте или удаляйте пользователей

### 4. Мониторинг подписок

- Перейдите в раздел "Подписки"
- Фильтруйте по активности
- Просматривайте запросы на возврат

---

## 📋 Примеры запросов

### Получить статистику:
```bash
curl -X GET "http://89.104.74.123:3000/api/admin/stats" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

### Получить список пользователей:
```bash
curl -X GET "http://89.104.74.123:3000/api/admin/users?skip=0&limit=50" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

### Получить активные подписки:
```bash
curl -X GET "http://89.104.74.123:3000/api/admin/subscriptions?active_only=true" \
  -H "Authorization: Bearer YOUR_ADMIN_TOKEN"
```

---

## ✅ Готово

Админ-панель полностью реализована и готова к использованию!

