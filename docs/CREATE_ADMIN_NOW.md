# 🚀 Создание администратора - Прямо сейчас!

## 📋 Шаг 1: Выберите email и пароль

Например:
- **Email:** `admin@dnevnik-uspekha.ru`
- **Пароль:** `SecurePassword123!`

---

## 📋 Шаг 2: Сгенерируйте SQL команду

Выполните локально:

```bash
cd /path/to/project
bash scripts/generate_admin_sql.sh ваш_email@example.com ваш_пароль
```

Скрипт выведет готовую SQL команду.

---

## 📋 Шаг 3: Выполните SQL в базе данных

**Вам нужно найти способ подключиться к PostgreSQL на сервере.**

### Варианты подключения:

**A. Если знаете параметры подключения:**
```bash
ssh root@89.104.74.123
psql -U postgres -d dnevnik_uspekha -h localhost
```

**B. Если база данных на другом сервере:**
```bash
psql -U username -d database_name -h hostname
```

**C. Если используете Docker:**
```bash
docker exec -it <container_name> psql -U postgres -d dnevnik_uspekha
```

**D. Если база данных в облаке:**
- Используйте веб-интерфейс вашего провайдера (например, AWS RDS, DigitalOcean, etc.)

---

## 📋 Шаг 4: Выполните SQL команду

Скопируйте SQL команду из шага 2 и выполните в psql или веб-интерфейсе.

---

## 📋 Шаг 5: Проверьте

```sql
SELECT email, role FROM users WHERE email = 'ваш_email@example.com';
```

Должно быть: `role = 'admin'`

---

## 📋 Шаг 6: Войдите в админку

1. Откройте: `http://89.104.74.123:3000`
2. Войдите с email и паролем администратора
3. Откройте: `http://89.104.74.123:3000/admin.html`

---

## ⚡ Быстрый вариант (если пользователь уже есть)

Если у вас уже есть аккаунт, просто обновите роль:

```sql
UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
```

---

## ❓ Не знаете как подключиться к базе?

**Варианты:**

1. **Проверьте конфигурацию backend:**
   - Найдите файл `.env` на сервере
   - Там должен быть `DATABASE_URL`

2. **Проверьте документацию деплоя:**
   - См. `docs/DEPLOY.md`
   - Там могут быть инструкции по базе данных

3. **Спросите у того, кто настраивал сервер**

---

## 🎯 Минимальная команда (если знаете пароль от PostgreSQL)

```bash
ssh root@89.104.74.123
psql -U postgres -d dnevnik_uspekha -c "UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';"
```

---

## ✅ После создания

1. Войдите на главной странице
2. Откройте админку: `http://89.104.74.123:3000/admin.html`
3. Готово! 🎉



