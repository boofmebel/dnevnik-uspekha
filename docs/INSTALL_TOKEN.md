# 🔐 Автоматическая установка токена

## ✅ Токен готов к использованию

Токен создан и проверен. Теперь нужно установить его в браузере.

## 🚀 Способ 1: Автоматическая установка (рекомендуется)

### Вариант A: Через специальную страницу

1. Откройте в браузере:
   ```
   http://localhost:8080/setup_token.html
   ```
   или
   ```
   http://89.104.74.123:8080/setup_token.html
   ```

2. Страница автоматически установит токен и проверит его

3. Нажмите кнопку "Перейти в админку"

### Вариант B: Через консоль браузера

1. Откройте админку: `http://89.104.74.123:3000/admin.html`

2. Нажмите **F12** (откроется консоль)

3. Скопируйте и выполните этот код:

```javascript
localStorage.setItem('admin_token', 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxIiwicm9sZSI6ImFkbWluIiwiZXhwIjoxNzY2MTM3NTI4LCJ0eXBlIjoiYWNjZXNzIn0.hD3XHJQ-a2MjIA-LLzk6s4KzgSC4Fdo9JUybWwkwRnM');
console.log('✅ Токен установлен!');
location.reload();
```

4. Страница автоматически обновится и токен будет использован

## 📋 Токен для копирования

```
eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJzdWIiOiIxIiwicm9sZSI6ImFkbWluIiwiZXhwIjoxNzY2MTM3NTI4LCJ0eXBlIjoiYWNjZXNzIn0.hD3XHJQ-a2MjIA-LLzk6s4KzgSC4Fdo9JUybWwkwRnM
```

## ✅ Проверка установки

После установки токена проверьте:

```javascript
// В консоли браузера (F12)
console.log('Токен установлен:', localStorage.getItem('admin_token') ? '✅ Да' : '❌ Нет');
```

## 🐛 Решение проблем

### Токен не работает (401 ошибка)

1. Проверьте, что токен установлен:
   ```javascript
   localStorage.getItem('admin_token')
   ```

2. Проверьте, что backend сервер работает:
   ```bash
   curl http://localhost:8000/api/docs
   ```

3. Проверьте логи сервера:
   ```bash
   tail -f /tmp/backend_server.log
   ```

### База данных недоступна

Если видите ошибку "Connection refused" - нужно запустить PostgreSQL:

```bash
# Проверьте, запущен ли PostgreSQL
pg_isready

# Если нет, запустите его (зависит от вашей системы)
# macOS:
brew services start postgresql

# Linux:
sudo systemctl start postgresql
```

## 📝 Статус

- ✅ Токен создан
- ✅ Токен проверен (валиден)
- ✅ Backend сервер запущен
- ⚠️ Требуется установка токена в браузер
- ⚠️ Требуется запущенная база данных для полной работы

