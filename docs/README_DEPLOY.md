# 🚀 Быстрая настройка автоматического деплоя

## Вариант 1: Простой (рекомендуется)

Использует GitHub CLI - самый простой способ:

```bash
# 1. Установите GitHub CLI (если ещё не установлен)
brew install gh  # macOS
# или https://cli.github.com для других ОС

# 2. Аутентифицируйтесь
gh auth login

# 3. Запустите скрипт
./scripts/setup_auto_deploy_simple.sh
```

Скрипт автоматически:
- ✅ Создаст SSH ключ
- ✅ Попросит ввести данные сервера
- ✅ Добавит все секреты в GitHub
- ✅ Покажет инструкции по настройке сервера

## Вариант 2: Полный (с поддержкой GitHub API)

Если нет GitHub CLI, но есть Personal Access Token:

```bash
./scripts/setup_auto_deploy.sh
```

Требования:
- Python 3 с библиотекой PyNaCl: `pip3 install pynacl`
- GitHub Personal Access Token с правами: `repo`, `admin:repo_hook`

## Что делает скрипт?

1. **Создаёт SSH ключ** для подключения к серверу
2. **Добавляет секреты в GitHub**:
   - `SERVER_HOST` - IP или домен сервера
   - `SERVER_USER` - пользователь SSH
   - `SERVER_SSH_KEY` - приватный SSH ключ
   - `SERVER_PATH` - путь к проекту на сервере
   - `SERVER_PORT` - SSH порт (по умолчанию 22)

3. **Показывает инструкции** по настройке сервера

## После запуска скрипта

### 1. Добавьте SSH ключ на сервер

```bash
ssh-copy-id -i ~/.ssh/id_ed25519_github_deploy.pub user@your-server.com
```

### 2. Подготовьте сервер

```bash
ssh user@your-server.com
mkdir -p /var/www/dnevnik-uspekha
cd /var/www/dnevnik-uspekha
git clone https://github.com/boofmebel/dnevnik-uspekha.git .
```

### 3. Протестируйте деплой

```bash
git push origin main
```

Проверьте результат: https://github.com/boofmebel/dnevnik-uspekha/actions

## Проверка секретов

Убедитесь, что все секреты добавлены:

```bash
gh secret list --repo boofmebel/dnevnik-uspekha
```

Должны быть:
- ✅ SERVER_HOST
- ✅ SERVER_USER
- ✅ SERVER_SSH_KEY
- ✅ SERVER_PATH
- ✅ SERVER_PORT

## Устранение проблем

### Ошибка: "gh: command not found"
Установите GitHub CLI: https://cli.github.com

### Ошибка: "Authentication required"
Выполните: `gh auth login`

### Ошибка при добавлении секрета
Проверьте права доступа к репозиторию. Нужны права администратора или настройка секретов.

### Деплой не запускается
1. Проверьте, что workflow файл существует: `.github/workflows/deploy.yml`
2. Проверьте, что все секреты добавлены
3. Проверьте логи в GitHub Actions

## Ручная настройка

Если скрипт не работает, настройте вручную:

1. Перейдите: https://github.com/boofmebel/dnevnik-uspekha/settings/secrets/actions
2. Добавьте секреты вручную (см. `docs/GITHUB_SECRETS_SETUP.md`)



