# 🔐 Настройка GitHub Secrets для деплоя

## 📋 Требуемые Secrets

Для работы автоматического деплоя необходимо настроить следующие Secrets в GitHub:

### Обязательные Secrets:

1. **SERVER_HOST** - IP адрес или домен сервера
   - Пример: `89.104.74.123`

2. **SERVER_USER** - Имя пользователя для SSH подключения
   - Пример: `root` или `deploy`

3. **SERVER_SSH_KEY** - Приватный SSH ключ для подключения к серверу
   - Полный приватный ключ (включая `-----BEGIN OPENSSH PRIVATE KEY-----` и `-----END OPENSSH PRIVATE KEY-----`)
   - Должен соответствовать публичному ключу, добавленному в `~/.ssh/authorized_keys` на сервере

4. **SERVER_PATH** - Путь к директории проекта на сервере
   - Пример: `/var/www/dnevnik-uspekha`

### Опциональные Secrets:

5. **SERVER_PORT** - SSH порт (по умолчанию 22)
   - Пример: `22`

## 🔧 Как настроить Secrets в GitHub

1. Перейдите в репозиторий: `https://github.com/boofmebel/dnevnik-uspekha`
2. Откройте: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**
4. Добавьте каждый Secret по отдельности

## ✅ Проверка настройки

После настройки Secrets, деплой должен запускаться автоматически при push в:
- `main` → продакшн (порт 3000)
- `dev` → тестовый сервер (порт 8080)

## 🚨 Частые проблемы

### 1. Деплой падает на шаге "Deploy via SSH"
- Проверьте, что SSH ключ правильный
- Убедитесь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
- Проверьте, что пользователь имеет права на выполнение команд

### 2. Ошибка "Permission denied"
- Убедитесь, что пользователь имеет права на запись в `SERVER_PATH`
- Проверьте права на выполнение команд (sudo может потребоваться для перезапуска сервисов)

### 3. DATABASE_URL не найден
- Убедитесь, что файл `backend/.env` существует на сервере
- Проверьте, что в `.env` указан правильный `DATABASE_URL`

## 📝 Пример .env файла на сервере

```bash
# backend/.env
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/dnevnik_prod
SECRET_KEY=your-secret-key-here
ENVIRONMENT=production
```

