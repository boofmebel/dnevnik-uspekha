# Промпт для улучшения системы мотивации ребенка

## Контекст проекта

Это веб-приложение для мотивации ребенка через систему задач, звезд и карманных денег. Приложение написано на чистом HTML/CSS/JavaScript, данные хранятся в localStorage.

**Структура проекта:**
- `index.html` - основная страница с интерфейсом
- `app.js` - логика приложения (697 строк)
- `styles.css` - стили

**Текущая система:**
- Ребенок выполняет задачи в чек-листе
- За каждую задачу получает 1-2 звезды
- 15 звезд = 200 рублей (автоматически попадают в копилку)
- В копилке можно установить цель (например, "Наушники 3000₽")
- Ежедневный сброс задач (звезды сбрасываются, но total сохраняется)

**Текущие константы в app.js (строки 35-37):**
```javascript
const STARS_TO_MONEY = 15;
const MONEY_PER_STARS = 200;
```

## Задача: Добавить улучшения системы мотивации

Нужно добавить следующие функции для улучшения мотивации ребенка:

### 1. Бонусы за серии дней (Streak Bonus)
- **Функционал:** Отслеживать, сколько дней подряд ребенок выполняет все задачи (или минимальное количество звезд)
- **Награды:**
  - 3 дня подряд = +10₽ бонус
  - 7 дней подряд = +50₽ бонус
  - 14 дней подряд = +150₽ бонус
  - 30 дней подряд = +500₽ бонус
- **Требования:**
  - Добавить в `appData` поле `streak: { current: 0, lastDate: null, best: 0 }`
  - При ежедневном сбросе проверять: если вчера было выполнено минимум 4 задачи (или 4+ звезд), увеличить streak
  - Если пропущен день, сбросить streak до 0
  - Показывать текущую серию в разделе "Звёзды сегодня" с эмодзи 🔥
  - Автоматически добавлять бонус в копилку при достижении бонусных отметок
  - Добавить в историю копилки запись типа "🔥 Бонус за 7 дней подряд: +50₽"

### 2. Настройка курса обмена в админке
- **Функционал:** В админ-панели добавить возможность изменить курс обмена звезд на деньги
- **Требования:**
  - Добавить в админку (строки 159-197 в index.html) секцию "⚙️ Настройки курса"
  - Два поля ввода: "Звезд для выплаты" и "Рублей за выплату"
  - Кнопка "Сохранить настройки"
  - Изменить константы `STARS_TO_MONEY` и `MONEY_PER_STARS` на переменные в `appData.settings`
  - Сохранять настройки в localStorage
  - Обновлять отображение "15 ⭐ = 200 ₽" динамически из настроек
  - При изменении настроек пересчитывать прогресс-бары

### 3. Промежуточные мини-награды (Quick Rewards)
- **Функционал:** Давать небольшие нематериальные награды за достижение промежуточных отметок
- **Награды:**
  - 5 звезд накоплено = 🎉 "Можешь выбрать мультик на вечер!"
  - 10 звезд накоплено = 🎁 "Маленький сюрприз" (показывать уведомление)
  - 25 звезд накоплено = 🌟 "Отличная работа!"
- **Требования:**
  - Добавить в `appData.stars` поле `claimedRewards: []` для отслеживания полученных наград
  - При достижении отметки (5, 10, 25) показывать модальное окно с поздравлением и описанием награды
  - Помечать награду как полученную, чтобы не показывать повторно
  - Добавить красивую анимацию при показе награды (можно использовать emoji и CSS анимации)
  - Модальное окно должно иметь кнопку "Понятно" для закрытия

### 4. Улучшенная статистика недели
- **Функционал:** Добавить страницу "Статистика" с графиками и отчетами
- **Требования:**
  - Добавить новый раздел в меню (иконка 📊) и страницу `page-stats`
  - Показывать:
    - Звезд за эту неделю vs прошлая неделя
    - Дней подряд (streak)
    - Среднее звезд в день
    - Прогресс к цели копилки
    - График звезд по дням недели (можно простыми CSS блоками или текстовым графиком)
  - Добавить в `appData` поле `weeklyStats: []` для хранения истории по неделям
  - Автоматически сохранять итоги недели в воскресенье вечером

## Технические требования

1. **Совместимость:** Код должен работать в современных браузерах (Chrome, Safari, Firefox)
2. **Стиль кода:** Следовать существующему стилю в app.js (функции, именование переменных)
3. **Сохранение данных:** Все новые данные должны сохраняться в localStorage через `saveData()`
4. **UI/UX:** 
   - Использовать существующие CSS классы из styles.css
   - Добавить новые стили если нужно, но в том же стиле (розовая тема, скругленные углы)
   - Модальные окна использовать существующий класс `.modal`
5. **Обратная совместимость:** Старые данные в localStorage должны корректно загружаться (добавить проверки на существование полей)

## Структура данных appData (нужно дополнить)

```javascript
let appData = {
  checklist: [],
  kanban: { todo: [], doing: [], done: [] },
  piggy: {
    amount: 0,
    goal: { name: '', amount: 0 },
    history: []
  },
  stars: {
    today: 0,
    total: 0,
    history: [],
    claimedRewards: [] // НОВОЕ: для мини-наград
  },
  money: {
    total: 0,
    history: []
  },
  streak: { // НОВОЕ: для серий дней
    current: 0,
    lastDate: null,
    best: 0
  },
  settings: { // НОВОЕ: настройки курса
    starsToMoney: 15,
    moneyPerStars: 200
  },
  weeklyStats: [], // НОВОЕ: статистика по неделям
  rules: [...],
  lastResetDate: null
};
```

## Важные моменты

1. **Ежедневный сброс:** Функция `resetDailyTasks()` (строка 79) должна проверять streak
2. **Функция `checkMoneyReward()`:** Обновить для использования `appData.settings` вместо констант
3. **Функция `updateStars()`:** Добавить проверку промежуточных наград
4. **Админка:** Добавить новые поля в существующую админ-панель (строки 151-198 в index.html)

## Приоритеты реализации

1. **Высокий приоритет:** Бонусы за серии дней (очень мотивирует)
2. **Высокий приоритет:** Настройка курса в админке (удобство для родителей)
3. **Средний приоритет:** Промежуточные мини-награды (дополнительная мотивация)
4. **Низкий приоритет:** Статистика недели (можно добавить позже, если нужно)

## Примеры использования

После реализации должно работать так:

1. Ребенок выполняет задачи 7 дней подряд → автоматически получает +50₽ в копилку с сообщением "🔥 Бонус за 7 дней подряд!"
2. Родитель меняет курс в админке на "10 ⭐ = 100 ₽" → система пересчитывает все прогресс-бары
3. Ребенок накапливает 10 звезд → появляется модальное окно "🎁 Поздравляем! Маленький сюрприз!"

## Вопросы для уточнения (если нужно)

- Нужны ли уведомления на устройстве (браузерные уведомления)?
- Нужна ли возможность отключить streak bonus в настройках?
- Какие именно "маленькие сюрпризы" показывать? (можно оставить на усмотрение родителей через админку)

---

**Примечание:** Реализуй все функции последовательно, тестируя каждую перед переходом к следующей. Убедись, что все данные корректно сохраняются и загружаются из localStorage.
