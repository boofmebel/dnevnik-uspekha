# 🚀 Быстрая настройка GitHub Actions

## Пошаговая инструкция

### 1️⃣ Создайте SSH ключ (если еще нет)

```bash
# На вашем компьютере
ssh-keygen -t ed25519 -C "github-actions-deploy"
# Нажмите Enter для всех вопросов (или укажите пароль)

# Скопируйте ПУБЛИЧНЫЙ ключ на сервер
ssh-copy-id -i ~/.ssh/id_ed25519.pub user@your-server.com

# Или вручную:
cat ~/.ssh/id_ed25519.pub
# Скопируйте вывод и добавьте в ~/.ssh/authorized_keys на сервере
```

### 2️⃣ Подготовьте сервер

```bash
# Подключитесь к серверу
ssh user@your-server.com

# Создайте директорию для проекта
sudo mkdir -p /var/www/dnevnik-uspekha
sudo chown -R $USER:$USER /var/www/dnevnik-uspekha
cd /var/www/dnevnik-uspekha

# Клонируйте репозиторий (или инициализируйте git)
git clone https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git .
# ИЛИ если репозиторий уже есть:
git init
git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
```

### 3️⃣ Настройте Secrets в GitHub

1. Откройте ваш репозиторий на GitHub
2. Перейдите: **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **"New repository secret"**
4. Добавьте секреты:

#### SERVER_HOST
```
Name: SERVER_HOST
Value: ваш-сервер.com
или
Value: 192.168.1.100
```

#### SERVER_USER
```
Name: SERVER_USER
Value: root
или
Value: deploy
```

#### SERVER_SSH_KEY
```
Name: SERVER_SSH_KEY
Value: [содержимое приватного ключа]

Получить содержимое:
cat ~/.ssh/id_ed25519

Скопируйте ВСЁ включая:
-----BEGIN OPENSSH PRIVATE KEY-----
...
-----END OPENSSH PRIVATE KEY-----
```

#### SERVER_PATH
```
Name: SERVER_PATH
Value: /var/www/dnevnik-uspekha
```

### 4️⃣ Проверьте workflow файл

Файл `.github/workflows/deploy.yml` уже создан и готов к использованию.

### 5️⃣ Тестирование

```bash
# Сделайте небольшое изменение
echo "# Test" >> README.md

# Закоммитьте и запушьте в main
git add .
git commit -m "Test deployment"
git push origin main
```

### 6️⃣ Проверьте результат

1. Перейдите в GitHub → **Actions**
2. Вы увидите запущенный workflow
3. Если всё настроено правильно, будет зелёная галочка ✅

## ✅ Готово!

Теперь каждый раз при `git push origin main` будет автоматически выполняться деплой на сервер.

## 🔧 Настройка веб-сервера (опционально)

Если нужно настроить nginx/apache для отдачи статических файлов:

```nginx
# /etc/nginx/sites-available/dnevnik-uspekha
server {
    listen 80;
    server_name ваш-домен.com;
    root /var/www/dnevnik-uspekha;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
```

