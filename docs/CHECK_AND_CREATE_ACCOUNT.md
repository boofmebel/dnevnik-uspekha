# 🔍 Проверка аккаунта и создание администратора

## ⚠️ Важно: Регистрация через номер телефона НЕ поддерживается

Система использует только **email и пароль** для регистрации.

---

## Шаг 1: Проверьте, есть ли у вас аккаунт

### Вариант A: Через API (если backend запущен)

Попробуйте зарегистрироваться или войти:

```bash
# Попробуйте зарегистрироваться
curl -X POST http://89.104.74.123:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"ваш_email@example.com","password":"ваш_пароль"}'
```

Если получите ошибку "Email already exists" - значит аккаунт есть!

### Вариант B: Через SQL (если есть доступ к базе)

```bash
ssh root@89.104.74.123
psql -U postgres -d dnevnik_uspekha
```

Затем:
```sql
SELECT id, email, role, created_at FROM users ORDER BY created_at DESC;
```

---

## Шаг 2: Если аккаунта нет - создайте его

### Способ 1: Через API (рекомендуется)

```bash
curl -X POST http://89.104.74.123:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "ваш_email@example.com",
    "password": "ваш_пароль_минимум_8_символов"
  }'
```

Если успешно - получите токен и данные пользователя.

### Способ 2: Через веб-интерфейс (если есть форма регистрации)

1. Откройте: `http://89.104.74.123:3000`
2. Найдите форму регистрации
3. Введите email и пароль
4. Зарегистрируйтесь

### Способ 3: Через SQL (если нужен хеш пароля)

См. инструкцию в `docs/CREATE_ADMIN_NOW.md`

---

## Шаг 3: Сделайте себя администратором

После регистрации выполните SQL:

```sql
UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
```

Или используйте скрипт (если есть Python окружение):
```bash
python3 scripts/create_admin.py ваш_email@example.com ваш_пароль
```

---

## Шаг 4: Войдите в админку

1. Откройте: `http://89.104.74.123:3000`
2. Войдите с вашим email и паролем
3. Откройте: `http://89.104.74.123:3000/admin.html`

---

## 📋 Создание других администраторов через админку

После того, как вы стали администратором:

1. **Откройте админку:** `http://89.104.74.123:3000/admin.html`
2. **Перейдите в раздел "Пользователи"**
3. **Найдите пользователя** (или создайте нового через API)
4. **Измените роль на "admin"** через интерфейс админки

Или через API:
```bash
curl -X PUT http://89.104.74.123:3000/api/admin/users/{user_id} \
  -H "Authorization: Bearer YOUR_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"role": "admin"}'
```

---

## ✅ План действий

1. ✅ Проверьте, есть ли аккаунт (см. Шаг 1)
2. ✅ Если нет - создайте через регистрацию (см. Шаг 2)
3. ✅ Сделайте себя администратором (см. Шаг 3)
4. ✅ Войдите в админку (см. Шаг 4)
5. ✅ Через админку создавайте других администраторов

---

## 🔍 Проверка регистрации

Проверьте, работает ли регистрация:

```bash
# Проверка endpoint
curl -X POST http://89.104.74.123:3000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@test.com","password":"test12345"}' \
  -v
```

Если получаете ошибку - проверьте:
- Запущен ли backend сервер
- Правильно ли настроена база данных
- Есть ли endpoint `/api/auth/register`

---

## 📞 Если что-то не работает

1. Проверьте логи backend:
   ```bash
   ssh root@89.104.74.123
   journalctl -u your-backend-service -n 50
   ```

2. Проверьте доступность API:
   ```bash
   curl http://89.104.74.123:3000/api/health
   ```

3. См. другие инструкции:
   - `docs/ADMIN_ACCESS_STEP_BY_STEP.md`
   - `docs/CREATE_ADMIN_NOW.md`



