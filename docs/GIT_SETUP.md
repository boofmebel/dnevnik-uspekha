# Быстрая настройка Git и GitHub

## ✅ Что уже сделано:
- ✅ Git репозиторий инициализирован
- ✅ Созданы ветки `main` и `dev`
- ✅ Создан начальный коммит
- ✅ Создан `.gitignore`
- ✅ Создан скрипт деплоя `deploy.sh`

## 📋 Следующие шаги:

### 1. Подключение к GitHub

```bash
# Создайте новый репозиторий на GitHub (через веб-интерфейс)
# Затем выполните:

git remote add origin https://github.com/ВАШ_USERNAME/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git

# Отправьте обе ветки
git push -u origin main
git checkout dev
git push -u origin dev
git checkout main
```

### 2. Рабочий процесс

**Разработка (ветка dev):**
```bash
git checkout dev
# Делаете изменения
git add .
git commit -m "Описание изменений"
git push origin dev
```

**Релиз (ветка main):**
```bash
git checkout main
git merge dev          # Мержим изменения из dev
git push origin main   # Автоматически деплоится на сервер
```

### 3. Деплой на сервер

**Автоматический (через GitHub Actions):**
- См. инструкции в `DEPLOY.md`

**Ручной:**
```bash
./deploy.sh user@your-server.com:/path/to/project
```

## 🔒 Важно:
- **main** - только стабильный код, автоматически деплоится
- **dev** - ветка для разработки
- Всегда мержите dev → main перед релизом

