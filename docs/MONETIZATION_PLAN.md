# 💰 План монетизации и архитектура приложения

## 🎯 Цель
Превратить приложение в SaaS-платформу с ролями:
- **Администратор** - полный доступ ко всем данным
- **Родители** - настройка для своего ребенка
- **Ребенок** - только использование

---

## 🏗️ АРХИТЕКТУРА

### Текущее состояние:
- ✅ Frontend: HTML/CSS/JS (SPA)
- ✅ Хранение: localStorage (локально)
- ✅ Один пользователь

### Целевое состояние:
- ✅ Frontend: React/Vue или чистый JS (SPA)
- ✅ Backend: Node.js/Express или Python/FastAPI
- ✅ База данных: PostgreSQL/MySQL
- ✅ Аутентификация: JWT токены
- ✅ Платежи: Stripe/YooKassa
- ✅ Хостинг: VPS/Cloud

---

## 👥 СИСТЕМА РОЛЕЙ

### 1. Администратор (Вы)
**Доступ:**
- Просмотр всех пользователей
- Статистика по всем детям
- Управление подписками
- Настройка тарифов
- Аналитика и отчеты
- Модерация контента

**Эндпоинты:**
```
GET  /api/admin/users              - список всех пользователей
GET  /api/admin/stats              - общая статистика
GET  /api/admin/subscriptions      - все подписки
PUT  /api/admin/pricing            - изменение тарифов
GET  /api/admin/analytics          - аналитика
```

### 2. Родитель
**Доступ:**
- Создание профиля ребенка
- Настройка задач и правил
- Установка курса обмена (звезды → деньги)
- Просмотр статистики ребенка
- Управление копилкой и целями
- История операций
- Настройка подписки

**Эндпоинты:**
```
POST   /api/parent/child           - создать профиль ребенка
GET    /api/parent/children         - список детей
PUT    /api/parent/child/:id        - обновить профиль
POST   /api/parent/tasks            - создать задачу
PUT    /api/parent/tasks/:id        - обновить задачу
POST   /api/parent/rules            - создать правило
PUT    /api/parent/settings/:id     - настройки курса
GET    /api/parent/stats/:childId   - статистика ребенка
POST   /api/parent/piggy/:childId   - пополнить копилку
GET    /api/parent/subscription     - подписка
POST   /api/parent/payment          - оплата
```

### 3. Ребенок
**Доступ:**
- Выполнение задач
- Просмотр звезд и прогресса
- Использование копилки
- Запись в дневник
- Просмотр вишлиста
- Конвертация звезд в деньги

**Эндпоинты:**
```
GET    /api/child/profile            - профиль
POST   /api/child/tasks/:id/complete - выполнить задачу
GET    /api/child/stars              - звезды
POST   /api/child/stars/convert      - конвертация
GET    /api/child/piggy              - копилка
POST   /api/child/diary              - запись в дневник
GET    /api/child/wishlist           - вишлист
```

---

## 💳 МОДЕЛИ ПОДПИСКИ

### Вариант 1: Ежемесячная подписка
- **Бесплатно:** 1 ребенок, базовые функции
- **Базовый:** 299₽/мес - 3 ребенка, все функции
- **Премиум:** 599₽/мес - неограниченно детей, аналитика, приоритетная поддержка

### Вариант 2: Пожизненная лицензия
- **Одноразово:** 2999₽ - 1 семья, все функции навсегда

### Вариант 3: Гибрид
- **Бесплатно:** 1 ребенок, ограниченные функции
- **Подписка:** 299₽/мес - полный доступ
- **Пожизненно:** 4999₽ - полный доступ навсегда

---

## 🗄️ СТРУКТУРА БАЗЫ ДАННЫХ

### Таблицы:

```sql
-- Пользователи
users (
  id UUID PRIMARY KEY,
  email VARCHAR(255) UNIQUE,
  password_hash VARCHAR(255),
  role ENUM('admin', 'parent', 'child'),
  parent_id UUID REFERENCES users(id), -- для детей
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)

-- Профили
profiles (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  name VARCHAR(255),
  avatar TEXT,
  gender ENUM('boy', 'girl', 'none'),
  created_at TIMESTAMP
)

-- Подписки
subscriptions (
  id UUID PRIMARY KEY,
  user_id UUID REFERENCES users(id),
  plan ENUM('free', 'basic', 'premium', 'lifetime'),
  status ENUM('active', 'cancelled', 'expired'),
  started_at TIMESTAMP,
  expires_at TIMESTAMP,
  payment_id VARCHAR(255)
)

-- Дети
children (
  id UUID PRIMARY KEY,
  parent_id UUID REFERENCES users(id),
  profile_id UUID REFERENCES profiles(id),
  settings JSONB, -- настройки курса, правила и т.д.
  created_at TIMESTAMP
)

-- Задачи
tasks (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  text TEXT,
  stars INTEGER,
  completed BOOLEAN,
  completed_at TIMESTAMP,
  date DATE,
  created_at TIMESTAMP
)

-- Звезды
stars (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  amount INTEGER,
  source VARCHAR(255), -- 'task', 'bonus', 'streak'
  date DATE,
  created_at TIMESTAMP
)

-- Копилка
piggy_bank (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  amount DECIMAL(10,2),
  goal_name VARCHAR(255),
  goal_amount DECIMAL(10,2),
  created_at TIMESTAMP,
  updated_at TIMESTAMP
)

-- История операций
transactions (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  type ENUM('earn', 'spend', 'convert'),
  amount DECIMAL(10,2),
  description TEXT,
  created_at TIMESTAMP
)

-- Правила
rules (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  text TEXT,
  created_at TIMESTAMP
)

-- Дневник
diary_entries (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  content TEXT,
  date DATE,
  created_at TIMESTAMP
)

-- Вишлист
wishlist (
  id UUID PRIMARY KEY,
  child_id UUID REFERENCES children(id),
  item TEXT,
  price DECIMAL(10,2),
  completed BOOLEAN,
  created_at TIMESTAMP
)
```

---

## 🔐 АУТЕНТИФИКАЦИЯ

### JWT токены:
```javascript
// При логине
POST /api/auth/login
{
  "email": "parent@example.com",
  "password": "password123"
}

// Ответ
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "uuid",
    "email": "parent@example.com",
    "role": "parent"
  }
}

// В заголовках всех запросов
Authorization: Bearer <token>
```

### Middleware для проверки ролей:
```javascript
// Проверка авторизации
authMiddleware(req, res, next) {
  const token = req.headers.authorization?.split(' ')[1];
  // Проверка JWT
  // Добавление user в req.user
}

// Проверка роли
roleMiddleware(['admin', 'parent'])(req, res, next) {
  if (!req.user.role in allowedRoles) {
    return 403;
  }
}
```

---

## 📱 ИНТЕРФЕЙСЫ

### 1. Лендинг (публичный)
- Описание приложения
- Тарифы
- Регистрация/Вход

### 2. Панель родителя
- Список детей
- Создание/редактирование профиля ребенка
- Настройка задач и правил
- Статистика
- Управление подпиской

### 3. Приложение ребенка
- Текущий интерфейс (упрощенный)
- Только выполнение задач
- Просмотр прогресса

### 4. Админ-панель
- Дашборд с метриками
- Управление пользователями
- Управление подписками
- Аналитика

---

## 💰 ИНТЕГРАЦИЯ ПЛАТЕЖЕЙ

### Вариант 1: Stripe (международный)
```javascript
POST /api/payment/create-checkout
{
  "plan": "basic",
  "amount": 29900 // в копейках
}

// Redirect на Stripe Checkout
// После оплаты webhook обновляет подписку
```

### Вариант 2: YooKassa (Россия)
```javascript
POST /api/payment/yookassa
{
  "plan": "basic",
  "amount": 299.00
}

// Возвращает payment_id
// Redirect на YooKassa
// Webhook обновляет подписку
```

---

## 🚀 ПЛАН РЕАЛИЗАЦИИ

### Этап 1: Backend (2-3 недели)
- [ ] Настройка Node.js/Express или Python/FastAPI
- [ ] Подключение PostgreSQL
- [ ] Миграции базы данных
- [ ] Модели данных
- [ ] JWT аутентификация
- [ ] API endpoints для всех ролей
- [ ] Middleware для проверки прав

### Этап 2: Frontend (2-3 недели)
- [ ] Разделение на страницы (логин, регистрация, панели)
- [ ] Интеграция с API
- [ ] Роутинг (React Router или нативный)
- [ ] Сохранение токена в localStorage
- [ ] Обработка ошибок авторизации
- [ ] Адаптация текущего UI под роли

### Этап 3: Платежи (1 неделя)
- [ ] Выбор платежной системы
- [ ] Интеграция API платежей
- [ ] Webhook для обновления подписок
- [ ] Страница управления подпиской

### Этап 4: Админ-панель (1-2 недели)
- [ ] Дашборд с метриками
- [ ] Управление пользователями
- [ ] Управление подписками
- [ ] Аналитика и отчеты

### Этап 5: Тестирование и запуск (1 неделя)
- [ ] Тестирование всех сценариев
- [ ] Исправление багов
- [ ] Оптимизация производительности
- [ ] Запуск в продакшн

---

## 📊 МЕТРИКИ ДЛЯ ОТСЛЕЖИВАНИЯ

- Количество регистраций
- Конверсия в платящих пользователей
- Средний чек
- Churn rate (отток)
- Активные пользователи (DAU/MAU)
- Среднее количество детей на аккаунт
- Среднее количество выполненных задач в день

---

## 🔒 БЕЗОПАСНОСТЬ

- Хеширование паролей (bcrypt)
- JWT с коротким временем жизни
- Refresh tokens
- Rate limiting
- Валидация всех входных данных
- HTTPS обязательно
- Защита от SQL injection
- CORS настройки

---

## 💡 РЕКОМЕНДАЦИИ

1. **Начните с MVP:**
   - Базовая аутентификация
   - Одна роль (родитель)
   - Простая подписка (месячная)
   - Минимум функций

2. **Используйте готовые решения:**
   - Auth0 или Firebase Auth для аутентификации
   - Stripe для платежей
   - Vercel/Netlify для фронтенда
   - Railway/Render для бэкенда

3. **Монетизация:**
   - Начните с бесплатного тарифа
   - Соберите пользователей
   - Добавьте платные функции постепенно

4. **Маркетинг:**
   - Лендинг с описанием
   - Социальные сети
   - Партнерская программа
   - Отзывы родителей

---

## 📝 СЛЕДУЮЩИЕ ШАГИ

1. Выберите стек технологий (Node.js или Python)
2. Создайте структуру проекта
3. Настройте базу данных
4. Реализуйте базовую аутентификацию
5. Мигрируйте текущий функционал на API

Готов помочь с реализацией любого этапа! 🚀

