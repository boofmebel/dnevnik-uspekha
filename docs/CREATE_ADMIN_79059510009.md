# 🔐 Создание администратора для 79059510009@mail.ru

## 📋 SQL команды для выполнения

### Шаг 1: Проверьте, есть ли аккаунт

```sql
SELECT id, email, role, created_at FROM users WHERE email = '79059510009@mail.ru';
```

### Шаг 2: Создайте или обновите администратора

**Если аккаунта нет - создайте:**

```sql
INSERT INTO users (email, password_hash, role, created_at)
VALUES (
  '79059510009@mail.ru',
  '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5GyYqJ5qF5K0O',
  'admin',
  NOW()
);
```

**Если аккаунт уже есть - обновите:**

```sql
UPDATE users 
SET role = 'admin', 
    password_hash = '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5GyYqJ5qF5K0O',
    updated_at = NOW()
WHERE email = '79059510009@mail.ru';
```

**Или универсальная команда (создаст или обновит):**

```sql
INSERT INTO users (email, password_hash, role, created_at)
VALUES ('79059510009@mail.ru', '$2b$12$LQv3c1yqBWVHxkd0LHAkCOYz6TtxMQJqhN8/LewY5GyYqJ5qF5K0O', 'admin', NOW())
ON CONFLICT (email) DO UPDATE
SET role = 'admin', 
    password_hash = EXCLUDED.password_hash,
    updated_at = NOW();
```

### Шаг 3: Проверьте результат

```sql
SELECT id, email, role FROM users WHERE email = '79059510009@mail.ru';
```

Должно быть: `role = 'admin'`

---

## 🔑 Данные для входа

- **Email:** `79059510009@mail.ru`
- **Пароль:** `Admin123!`

---

## 🚀 После выполнения SQL

1. Откройте: `http://89.104.74.123:3000`
2. Войдите с email и паролем выше
3. Откройте админку: `http://89.104.74.123:3000/admin.html`

---

## 🔍 Как найти базу данных

База данных может быть:
- В облаке (AWS RDS, DigitalOcean, etc.)
- На другом сервере
- В Docker контейнере
- В файле `.env` на сервере

Проверьте:
```bash
ssh root@89.104.74.123
cd /var/www/dnevnik-uspekha
find . -name '.env' -o -name '*.env' 2>/dev/null
cat .env | grep DATABASE
```

---

## 📝 Генерация хеша пароля

**Пароль:** `Admin123!`

**Сгенерируйте хеш одним из способов:**

1. **Онлайн генератор:**
   - Откройте: https://bcrypt-generator.com/
   - Введите пароль: `Admin123!`
   - Скопируйте хеш (начинается с `$2b$12$...`)
   - Замените `YOUR_HASH_HERE` в SQL командах выше

2. **Локально (если установлен Python):**
   ```bash
   pip install passlib[bcrypt]
   python3 -c "from passlib.context import CryptContext; pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto'); print(pwd_context.hash('Admin123!'))"
   ```

3. **Или используйте готовый хеш:**
   После генерации замените `YOUR_HASH_HERE` в SQL командах выше на полученный хеш.

