# 🏗️ Архитектура - Дневник успеха

Документация по архитектуре приложения согласно `rules.md`.

## 📁 Структура проекта

```
/
├── index.html              # Главный HTML файл
├── styles.css              # Стили приложения
├── manifest.json           # PWA манифест
│
├── error-handler.js        # Глобальная обработка ошибок
├── utils.js                # Утилиты (валидация, безопасность)
├── data.js                 # Работа с данными (localStorage)
├── ui.js                   # UI логика (рендеринг, модальные окна)
├── app.js                  # Инициализация приложения
│
├── health.html             # Health-check endpoint
│
├── nginx.conf              # Конфигурация Nginx
│
├── rules.md                # Правила разработки (Cursor Rules)
├── SECURITY.md             # Документация по безопасности
└── ARCHITECTURE.md         # Этот файл
```

## 🔄 Поток данных

### Инициализация приложения

```
1. Загрузка HTML (index.html)
   ↓
2. Загрузка error-handler.js (первым!)
   ↓
3. Загрузка utils.js (утилиты)
   ↓
4. Загрузка data.js (работа с данными)
   ↓
5. Загрузка ui.js (UI логика)
   ↓
6. Загрузка app.js (инициализация)
   ↓
7. DOMContentLoaded → инициализация
   - loadData() - загрузка из localStorage
   - migrateData() - миграция если нужно
   - initSettings() - инициализация настроек
   - initDefaultTasks() - задачи по умолчанию
   - checkDailyReset() - ежедневный сброс
   - render*() - первоначальный рендеринг
```

### Сохранение данных

```
Пользовательское действие
   ↓
Валидация (utils.js)
   ↓
Обновление appData (data.js)
   ↓
saveData()
   - Проверка размера
   - Сериализация JSON
   - Сохранение в localStorage
   - Обработка ошибок
   ↓
Обновление UI (ui.js)
```

## 🧩 Модули

### 1. error-handler.js

**Назначение:** Глобальная обработка ошибок и логирование.

**Функции:**
- `log(level, message, context)` - структурированное логирование
- `safeExecute(fn, context)` - безопасное выполнение функции
- `safeExecuteAsync(fn, context)` - безопасное выполнение async функции
- `handleStorageError(error, operation)` - обработка ошибок localStorage
- `initErrorHandling()` - инициализация обработчиков

**Использование:**
```javascript
// Логирование
window.errorHandler.log(window.errorHandler.LogLevel.ERROR, 'Error message', { context });

// Безопасное выполнение
const result = window.errorHandler.safeExecute(() => {
  // код, который может выбросить ошибку
}, 'functionName');
```

### 2. utils.js

**Назначение:** Утилиты для безопасности и валидации.

**Функции:**
- `sanitizeText(text)` - санитизация текста от XSS
- `validateTaskText(text)` - валидация текста задачи
- `validateNumber(value, options)` - валидация числа
- `validateEmail(email)` - валидация email
- `validateUrl(url)` - валидация URL
- `validateUsername(username)` - валидация имени пользователя
- `validateDate(dateString)` - валидация даты
- `validateImageFile(file)` - валидация файла изображения
- `createTextElement(tag, text, className)` - безопасное создание элемента
- `safeJsonParse(jsonString, defaultValue)` - безопасный парсинг JSON
- `checkStorageSize(data)` - проверка размера данных

### 3. data.js

**Назначение:** Управление состоянием приложения и работой с localStorage.

**Глобальная переменная:**
- `appData` - объект с данными приложения

**Функции:**
- `loadData()` - загрузка данных из localStorage
- `saveData()` - сохранение данных в localStorage
- `migrateData(data, fromVersion, toVersion)` - миграция данных
- `checkDailyReset()` - проверка ежедневного сброса
- `checkWeeklyReset()` - проверка еженедельного сброса
- `checkStreak()` - проверка серии дней
- `addStarHistory(description, stars)` - добавление в историю звёзд
- `addMoneyHistory(type, amount, description)` - добавление в историю денег
- `addMoneyToWallet(amount, description)` - добавление денег в кошелёк
- `getStarsToMoney()` - получение курса обмена
- `getMoneyPerStars()` - получение суммы за звёзды

**Структура appData:**
```javascript
{
  version: "2.0.0",           // Версия структуры данных
  checklist: [],              // Список задач
  kanban: {                   // Канбан доска
    todo: [],
    doing: [],
    done: []
  },
  piggy: {                    // Копилка
    amount: 0,
    goal: { name: "", amount: 0 },
    history: []
  },
  stars: {                    // Звёзды
    today: 0,
    total: 0,
    history: [],
    streak: { current: 0, lastDate: null, history: [] },
    claimedRewards: []
  },
  money: { total: 0, history: [] },
  wallet: { amount: 0, history: [] },
  rules: [],
  settings: { starsToMoney: 15, moneyPerStars: 200 },
  weeklyStats: { days: [], lastWeek: [] },
  diary: [],
  wishlist: [],
  profile: { avatar: null, name: "Ребёнок", gender: "none" },
  lastResetDate: null
}
```

### 4. ui.js

**Назначение:** Вся логика отображения и взаимодействия с пользователем.

**Основные функции:**
- `renderChecklist()` - отображение списка задач
- `renderKanban()` - отображение канбан доски
- `renderPiggy()` - отображение копилки
- `renderMoney()` - отображение денег
- `renderDiary()` - отображение дневника
- `renderWishlist()` - отображение списка желаний
- `renderWeeklyStats()` - отображение статистики недели
- `updateStars()` - обновление отображения звёзд
- `openPage(id, btn)` - навигация между страницами
- `openProfileModal()` - открытие модального окна профиля
- `openStarsExchangeModal()` - открытие модального окна обмена звёзд

**Принципы:**
- Все функции рендеринга используют безопасные методы (`createTextElement`, `textContent`)
- Никакого `innerHTML` для пользовательских данных
- Все пользовательские данные санитизируются перед отображением

### 5. app.js

**Назначение:** Инициализация приложения и связующая логика.

**Содержимое:**
- Обработчик `DOMContentLoaded`
- Последовательность инициализации:
  1. `loadData()` - загрузка данных
  2. `initSettings()` - инициализация настроек
  3. `initDefaultTasks()` - задачи по умолчанию
  4. `checkDailyReset()` - проверка ежедневного сброса
  5. Первоначальный рендеринг всех компонентов
  6. Применение сохранённых настроек (цвета, имя)
- Обработчики событий (Enter для форм, клики вне модальных окон)

## 🔐 Принципы безопасности

### 1. Защита от XSS

- ✅ Все пользовательские данные санитизируются
- ✅ Использование `textContent` вместо `innerHTML`
- ✅ Валидация всех входных данных

### 2. Валидация данных

- ✅ Валидация перед сохранением
- ✅ Валидация при загрузке
- ✅ Проверка размера данных

### 3. Обработка ошибок

- ✅ Глобальная обработка ошибок
- ✅ Структурированное логирование
- ✅ Безопасное отображение ошибок

## 📊 Версионирование данных

### Текущая версия: 2.0.0

**Изменения в версии 2.0.0:**
- Добавлено поле `version` в `appData`
- Добавлен объект `wallet` для хранения денег из обмена звёзд
- Добавлено поле `profile.gender` для выбора пола
- Добавлено поле `stars.claimedRewards` для отслеживания полученных наград

**Миграция:**
При загрузке данных проверяется версия. Если версия отличается от текущей, выполняется миграция через функцию `migrateData()`.

## 🚀 Деплой

### Структура на сервере

```
/var/www/dnevnik-uspekha/          # Продакшн (main ветка)
├── index.html
├── *.js
├── *.css
└── ...

/var/www/dnevnik-uspekha-test/     # Тест (dev ветка)
├── index.html
├── *.js
├── *.css
└── ...
```

### Nginx конфигурация

- **Продакшн:** HTTPS на порту 443, редирект с HTTP (80)
- **Тест:** HTTP на порту 8080 (можно настроить HTTPS)

### GitHub Actions

- Автоматический деплой при push в `main` (продакшн)
- Автоматический деплой при push в `dev` (тест)

## 🔄 Будущие улучшения

### Планируемые изменения:

1. **Реорганизация структуры:**
   ```
   /src/js/        # JavaScript файлы
   /static/css/    # CSS файлы
   /config/        # Конфигурационные файлы
   ```

2. **Backend интеграция:**
   - API клиент модуль
   - Абстракция работы с данными (localStorage → API)
   - JWT аутентификация

3. **Оптимизация:**
   - Минификация JS/CSS
   - Source maps для отладки
   - Lazy loading модулей

4. **Тестирование:**
   - Unit тесты для `utils.js`
   - Интеграционные тесты
   - Тесты безопасности

## 📝 Примечания

- Все модули загружаются синхронно в указанном порядке
- Зависимости между модулями явные (через глобальные переменные)
- В будущем можно перейти на ES6 модули для лучшей изоляции

