# 🔧 Устранение проблем с админкой

## ❌ Проблема: Вижу обычный интерфейс вместо админки

### Причина:
1. Файл `admin.html` не задеплоен на сервер
2. Открыта главная страница (`index.html`) вместо `admin.html`
3. Nginx редиректит на `index.html` из-за `try_files`

### Решение:

**1. Проверьте URL:**
```
✅ Правильно: http://89.104.74.123:3000/admin.html
❌ Неправильно: http://89.104.74.123:3000/admin
❌ Неправильно: http://89.104.74.123:3000/
```

**2. Задеплойте файлы админки:**
```bash
cd /path/to/project
bash scripts/deploy_admin.sh
```

**3. Проверьте наличие файлов на сервере:**
```bash
ssh root@89.104.74.123 "cd /var/www/dnevnik-uspekha && \
  test -f admin.html && echo '✅ admin.html' || echo '❌ admin.html' && \
  test -f src/js/admin.js && echo '✅ admin.js' || echo '❌ admin.js' && \
  test -f static/css/admin.css && echo '✅ admin.css' || echo '❌ admin.css'"
```

---

## ❌ Проблема: "Требуется авторизация"

### Причина:
Нет JWT токена для доступа к API.

### Решение:

**Вариант 1: Войти на главной странице**
1. Откройте: `http://89.104.74.123:3000`
2. Войдите как администратор
3. Откройте: `http://89.104.74.123:3000/admin.html`

**Вариант 2: Сохранить токен вручную**
1. На главной странице откройте консоль браузера (F12)
2. Выполните:
```javascript
localStorage.setItem('admin_token', apiClient.getAccessToken());
```
3. Откройте `admin.html`

---

## ❌ Проблема: "Требуются права администратора"

### Причина:
Пользователь не имеет роль `admin` в базе данных.

### Решение:

**1. Создайте администратора:**
```sql
-- Подключитесь к PostgreSQL
UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
```

**2. Выйдите и войдите заново**

**3. Откройте админку**

---

## ❌ Проблема: Файлы не загружаются (404 ошибки)

### Причина:
Файлы CSS/JS не скопированы на сервер.

### Решение:

**Запустите скрипт деплоя:**
```bash
bash scripts/deploy_admin.sh
```

Или вручную:
```bash
scp frontend/admin.html root@89.104.74.123:/var/www/dnevnik-uspekha/
scp frontend/src/js/admin.js root@89.104.74.123:/var/www/dnevnik-uspekha/src/js/
scp frontend/static/css/admin.css root@89.104.74.123:/var/www/dnevnik-uspekha/static/css/
```

---

## ❌ Проблема: Белый экран или ошибки в консоли

### Проверьте:

1. **Консоль браузера (F12):**
   - Откройте вкладку Console
   - Посмотрите на ошибки

2. **Сеть (Network):**
   - Откройте вкладку Network
   - Проверьте, загружаются ли файлы (admin.js, admin.css, api.js)

3. **Права доступа:**
```bash
ssh root@89.104.74.123 "chmod 644 /var/www/dnevnik-uspekha/admin.html"
```

---

## ✅ Правильный интерфейс админки

Админка должна выглядеть так:

```
┌─────────────────────────────────────┐
│ 🔐 Админ-панель    [Email] [Выход] │  ← Тёмный фон
├─────────────────────────────────────┤
│ [Dashboard] [Пользователи] [Дети]  │  ← Навигация
│ [Подписки] [Уведомления]            │
├─────────────────────────────────────┤
│ 📊 Статистика                       │
│ ┌─────┬─────┬─────┐                │
│ │ 👥  │ 👨  │ 👧  │                │  ← Карточки
│ │ 150 │ 100 │ 200 │                │
│ └─────┴─────┴─────┘                │
│                                     │
│ 👥 Последние пользователи           │
│ [Таблица]                           │  ← Таблицы
└─────────────────────────────────────┘
```

**НЕ должно быть:**
- ❌ Задачи и звёзды
- ❌ Копилка
- ❌ Навигация "Дела", "Задания"

---

## 🔍 Диагностика

### Проверка 1: Файл существует?
```bash
curl -I http://89.104.74.123:3000/admin.html
# Должен вернуть 200 OK
```

### Проверка 2: Файлы на сервере?
```bash
ssh root@89.104.74.123 "ls -la /var/www/dnevnik-uspekha/admin.html"
```

### Проверка 3: Консоль браузера
Откройте F12 → Console и посмотрите ошибки.

---

## 🚀 Быстрое решение

Если ничего не помогает:

```bash
# 1. Задеплойте админку
bash scripts/deploy_admin.sh

# 2. Создайте администратора
# (через базу данных)

# 3. Войдите на главной странице

# 4. Откройте admin.html
```

---

## 📞 Если проблема остаётся

Проверьте:
1. ✅ URL правильный (`/admin.html`)
2. ✅ Файлы на сервере
3. ✅ Роль пользователя = `admin`
4. ✅ Токен сохранён
5. ✅ Консоль браузера (ошибки)



