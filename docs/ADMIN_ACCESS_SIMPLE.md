# 🚀 Быстрый доступ к админке (пошаговая инструкция)

## 📋 Что нужно сделать

### Шаг 1: Выберите email и пароль для администратора

Например:
- Email: `admin@dnevnik-uspekha.ru`
- Пароль: `SecurePassword123!`

---

### Шаг 2: Создайте администратора

**Вариант A: Через Python скрипт (если есть доступ к серверу и Python)**

```bash
# Локально (если есть доступ к базе)
cd /path/to/project
python3 scripts/create_admin.py admin@dnevnik-uspekha.ru SecurePassword123!

# Или удалённо через SSH
ssh root@89.104.74.123 "cd /var/www/dnevnik-uspekha && python3 scripts/create_admin.py admin@dnevnik-uspekha.ru SecurePassword123!"
```

**Вариант B: Через SQL (если есть доступ к PostgreSQL)**

1. Подключитесь к базе данных:
   ```bash
   ssh root@89.104.74.123
   # Затем найдите способ подключиться к PostgreSQL
   ```

2. Выполните SQL:
   ```sql
   -- Если пользователь уже существует
   UPDATE users SET role = 'admin' WHERE email = 'admin@dnevnik-uspekha.ru';
   
   -- Или создайте нового (нужен хеш пароля)
   -- См. инструкцию ниже как получить хеш
   ```

**Вариант C: Через существующего пользователя (если уже есть аккаунт)**

1. Найдите свой email в базе данных
2. Обновите роль:
   ```sql
   UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
   ```

---

### Шаг 3: Войдите как администратор

1. Откройте главную страницу: `http://89.104.74.123:3000`
2. Войдите с email и паролем администратора
3. После входа токен сохранится автоматически

---

### Шаг 4: Откройте админку

1. Откройте: `http://89.104.74.123:3000/admin.html`
2. Админка должна загрузиться автоматически

---

## 🔧 Как получить хеш пароля для SQL

Если нужно создать нового пользователя через SQL, нужен хеш пароля:

```python
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto')
print(pwd_context.hash('your_password'))
```

Или через командную строку:
```bash
python3 -c "from passlib.context import CryptContext; pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto'); print(pwd_context.hash('your_password'))"
```

---

## ✅ Проверка

После создания администратора проверьте:

```sql
SELECT email, role FROM users WHERE role = 'admin';
```

Должен появиться ваш email с ролью `admin`.

---

## 🎯 Самый простой способ

**Если у вас уже есть аккаунт:**

1. Подключитесь к базе данных
2. Выполните:
   ```sql
   UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
   ```
3. Выйдите и войдите заново
4. Откройте `admin.html`

---

## 📞 Нужна помощь?

См. подробные инструкции:
- `docs/HOW_TO_GET_ADMIN_ACCESS.md` - Полная инструкция
- `docs/ADMIN_TROUBLESHOOTING.md` - Решение проблем



