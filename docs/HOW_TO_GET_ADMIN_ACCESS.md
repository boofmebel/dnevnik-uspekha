# 🔐 Как получить доступ к админке

## 📋 Варианты создания администратора

### Вариант 1: Через Python скрипт (рекомендуется) ⭐

**Если у вас есть доступ к серверу и Python окружению:**

```bash
cd /path/to/project
python3 scripts/create_admin.py your_email@example.com your_password
```

**Или удалённо через SSH:**
```bash
ssh root@89.104.74.123 "cd /var/www/dnevnik-uspekha && python3 scripts/create_admin.py your_email@example.com your_password"
```

---

### Вариант 2: Через SQL (если есть доступ к PostgreSQL)

**1. Подключитесь к базе данных:**
```bash
ssh root@89.104.74.123
psql -U postgres -d dnevnik_uspekha
```

**2. Выполните SQL:**
```sql
-- Если пользователь уже существует
UPDATE users 
SET role = 'admin' 
WHERE email = 'your_email@example.com';

-- Или создайте нового администратора
INSERT INTO users (email, password_hash, role, created_at)
VALUES (
  'admin@example.com',
  '$2b$12$...',  -- Хеш пароля (см. ниже как получить)
  'admin',
  NOW()
);
```

**3. Как получить хеш пароля:**
```python
from passlib.context import CryptContext
pwd_context = CryptContext(schemes=['bcrypt'], deprecated='auto')
print(pwd_context.hash('your_password'))
```

---

### Вариант 3: Через существующего пользователя (если уже есть аккаунт)

**1. Найдите свой email в базе:**
```sql
SELECT id, email, role FROM users;
```

**2. Обновите роль:**
```sql
UPDATE users SET role = 'admin' WHERE email = 'your_email@example.com';
```

---

## 🚀 Пошаговая инструкция

### Шаг 1: Создайте администратора

Выберите один из вариантов выше и создайте администратора.

### Шаг 2: Войдите как администратор

1. Откройте главную страницу: `http://89.104.74.123:3000`
2. Нажмите "Вход" (если есть) или используйте API:
   ```bash
   curl -X POST http://89.104.74.123:3000/api/auth/login \
     -H "Content-Type: application/json" \
     -d '{"email": "your_email@example.com", "password": "your_password"}'
   ```
3. Сохраните полученный `access_token`

### Шаг 3: Сохраните токен (временное решение)

В консоли браузера (F12) на главной странице после входа:

```javascript
localStorage.setItem('admin_token', apiClient.getAccessToken());
```

### Шаг 4: Откройте админку

1. Откройте: `http://89.104.74.123:3000/admin.html`
2. Админка должна загрузиться автоматически

---

## 🔍 Проверка

### Проверить, что пользователь администратор:

```sql
SELECT email, role FROM users WHERE email = 'your_email@example.com';
-- Должно быть: role = 'admin'
```

### Проверить список всех администраторов:

```sql
SELECT email, role, created_at FROM users WHERE role = 'admin';
```

---

## 🛠️ Быстрое решение

**Если у вас уже есть аккаунт:**

1. Подключитесь к базе данных
2. Выполните:
   ```sql
   UPDATE users SET role = 'admin' WHERE email = 'ваш_email@example.com';
   ```
3. Выйдите и войдите заново
4. Откройте `admin.html`

---

## 📝 Примеры

### Пример 1: Создать нового администратора

```bash
python3 scripts/create_admin.py admin@example.com secure_password123
```

### Пример 2: Обновить существующего пользователя

```sql
UPDATE users SET role = 'admin' WHERE email = 'existing_user@example.com';
```

### Пример 3: Через SSH

```bash
ssh root@89.104.74.123 "cd /var/www/dnevnik-uspekha && python3 scripts/create_admin.py admin@example.com password123"
```

---

## ⚠️ Важно

1. **Безопасность пароля:** Используйте надёжный пароль
2. **Один администратор:** Рекомендуется иметь минимум одного администратора
3. **Резервная копия:** Сохраните данные администратора в безопасном месте

---

## ✅ После создания администратора

1. Войдите на главной странице: `http://89.104.74.123:3000`
2. Откройте админку: `http://89.104.74.123:3000/admin.html`
3. Должен появиться интерфейс админки

---

## 🔗 Полезные ссылки

- `docs/ADMIN_ACCESS.md` - Подробная инструкция по доступу
- `docs/ADMIN_TROUBLESHOOTING.md` - Решение проблем
- `scripts/create_admin.py` - Скрипт для создания администратора

