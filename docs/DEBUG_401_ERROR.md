# 🔍 Отладка ошибки 401 (Unauthorized)

## Проблема
Токен передается в заголовках запроса, но сервер возвращает 401 Unauthorized.

## Возможные причины

### 1. SECRET_KEY не совпадает
Токен был создан с одним SECRET_KEY, а проверяется с другим.

**Решение:**
- Убедитесь, что SECRET_KEY одинаковый в `.env` файле
- Перезапустите backend сервер после изменения SECRET_KEY
- Войдите заново, чтобы получить новый токен

### 2. Токен истек
Токен может быть просрочен.

**Решение:**
- Войдите заново в админ-панель
- Токен действителен 60 минут (настроено в `ACCESS_TOKEN_EXPIRE_MINUTES`)

### 3. CORS проблемы
Сервер может блокировать запросы из-за CORS.

**Решение:**
- Проверьте, что `89.104.74.123:3000` добавлен в `ALLOWED_ORIGINS`
- Уже добавлено в код ✅

### 4. Проблема с декодированием токена
Токен может быть поврежден или неверного формата.

**Решение:**
- Проверьте логи backend сервера
- Убедитесь, что токен не обрезан

## Шаги для диагностики

### 1. Проверьте логи backend сервера
Ищите сообщения:
- "Verifying token..."
- "Token verified successfully"
- "Token verification failed"
- "JWT Error"

### 2. Проверьте SECRET_KEY
```bash
# На сервере
cd backend
cat .env | grep SECRET_KEY
```

### 3. Проверьте токен в браузере
```javascript
// В консоли браузера
const token = localStorage.getItem('admin_token');
console.log('Token length:', token?.length);
console.log('Token preview:', token?.substring(0, 50));
```

### 4. Войдите заново
1. Откройте главную страницу
2. Войдите как администратор
3. Откройте админ-панель

## Добавлено логирование

В код добавлено детальное логирование:
- ✅ Логирование проверки токена
- ✅ Логирование ошибок декодирования
- ✅ Логирование найденного пользователя
- ✅ Логирование CORS настроек

## Проверка на сервере

Проверьте логи backend при запросе:
```bash
# На сервере, где запущен backend
tail -f /var/log/your-app.log
# или
journalctl -u your-service -f
```

Ищите строки с "Verifying token" и "Token verified".

## Быстрое решение

1. **Очистите localStorage:**
   ```javascript
   localStorage.removeItem('admin_token');
   ```

2. **Войдите заново:**
   - Откройте главную страницу
   - Войдите как администратор
   - Откройте админ-панель

3. **Проверьте SECRET_KEY:**
   - Убедитесь, что SECRET_KEY установлен в `.env`
   - Перезапустите backend сервер

