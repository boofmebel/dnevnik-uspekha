# 📋 Подробная инструкция по настройке проекта

## 🎯 Цель
Настроить автоматический деплой проекта "Дневник успеха" на сервер с двумя окружениями:
- **Продакшн** (main ветка) → http://89.104.74.123
- **Тест** (dev ветка) → http://89.104.74.123:8080

---

## ✅ ЧЕК-ЛИСТ НАСТРОЙКИ

### ШАГ 1: Подготовка SSH ключей

- [ ] **1.1. Проверьте наличие SSH ключа**
  ```bash
  ls -la ~/.ssh/id_rsa.pub
  ```
  Если файла нет, создайте ключ:
  ```bash
  ssh-keygen -t rsa -b 4096 -C "your_email@example.com"
  # Нажмите Enter для всех вопросов (или укажите пароль)
  ```

- [ ] **1.2. Скопируйте публичный ключ**
  ```bash
  cat ~/.ssh/id_rsa.pub
  ```
  **Сохраните этот ключ** - он понадобится в шаге 2.3

- [ ] **1.3. Скопируйте приватный ключ**
  ```bash
  cat ~/.ssh/id_rsa
  ```
  **Сохраните этот ключ** - он понадобится в шаге 2.4

---

### ШАГ 2: Настройка GitHub Secrets

- [ ] **2.1. Откройте настройки репозитория**
  - Перейдите: https://github.com/boofmebel/dnevnik-uspekha
  - Нажмите: **Settings** → **Secrets and variables** → **Actions**

- [ ] **2.2. Добавьте секрет `SERVER_HOST`**
  - Нажмите: **New repository secret**
  - **Name:** `SERVER_HOST`
  - **Value:** `89.104.74.123`
  - Нажмите: **Add secret**

- [ ] **2.3. Добавьте секрет `SERVER_USER`**
  - Нажмите: **New repository secret**
  - **Name:** `SERVER_USER`
  - **Value:** `root` (или ваш пользователь на сервере)
  - Нажмите: **Add secret**

- [ ] **2.4. Добавьте секрет `SERVER_SSH_KEY`**
  - Нажмите: **New repository secret**
  - **Name:** `SERVER_SSH_KEY`
  - **Value:** Вставьте **ПРИВАТНЫЙ** ключ (из шага 1.3)
    ```
    -----BEGIN OPENSSH PRIVATE KEY-----
    ...ваш ключ...
    -----END OPENSSH PRIVATE KEY-----
    ```
  - Нажмите: **Add secret**

- [ ] **2.5. Добавьте секрет `SERVER_PATH`**
  - Нажмите: **New repository secret**
  - **Name:** `SERVER_PATH`
  - **Value:** `/var/www/dnevnik-uspekha`
  - Нажмите: **Add secret**

- [ ] **2.6. (Опционально) Добавьте секрет `SERVER_PORT`**
  - Нажмите: **New repository secret**
  - **Name:** `SERVER_PORT`
  - **Value:** `22` (стандартный порт SSH)
  - Нажмите: **Add secret**
  - ⚠️ Если не добавите, будет использован порт 22 по умолчанию

**Проверка:** В списке секретов должно быть минимум 4 секрета:
- ✅ SERVER_HOST
- ✅ SERVER_USER
- ✅ SERVER_SSH_KEY
- ✅ SERVER_PATH

---

### ШАГ 3: Настройка сервера

- [ ] **3.1. Подключитесь к серверу**
  ```bash
  ssh root@89.104.74.123
  ```

- [ ] **3.2. Установите nginx (если не установлен)**
  ```bash
  # Проверка
  nginx -v
  
  # Если не установлен:
  sudo apt-get update
  sudo apt-get install -y nginx
  ```

- [ ] **3.3. Установите git (если не установлен)**
  ```bash
  # Проверка
  git --version
  
  # Если не установлен:
  sudo apt-get install -y git
  ```

- [ ] **3.4. Создайте директории**
  ```bash
  sudo mkdir -p /var/www/dnevnik-uspekha
  sudo mkdir -p /var/www/dnevnik-uspekha-test
  sudo chown -R $USER:$USER /var/www/dnevnik-uspekha
  sudo chown -R $USER:$USER /var/www/dnevnik-uspekha-test
  ```

- [ ] **3.5. Добавьте SSH ключ на сервер**
  ```bash
  # На вашем локальном компьютере выполните:
  ssh-copy-id root@89.104.74.123
  
  # ИЛИ вручную на сервере:
  mkdir -p ~/.ssh
  chmod 700 ~/.ssh
  # Вставьте публичный ключ (из шага 1.2) в файл:
  nano ~/.ssh/authorized_keys
  chmod 600 ~/.ssh/authorized_keys
  ```

- [ ] **3.6. Клонируйте репозитории**
  ```bash
  # Продакшн (main ветка)
  cd /var/www/dnevnik-uspekha
  git clone https://github.com/boofmebel/dnevnik-uspekha.git .
  git checkout main
  
  # Тест (dev ветка)
  cd /var/www/dnevnik-uspekha-test
  git clone https://github.com/boofmebel/dnevnik-uspekha.git .
  git checkout dev
  ```

- [ ] **3.7. Настройте nginx**
  ```bash
  # Скопируйте конфигурацию
  sudo cp /var/www/dnevnik-uspekha/nginx.conf /etc/nginx/sites-available/dnevnik-uspekha
  
  # Создайте симлинк
  sudo ln -sf /etc/nginx/sites-available/dnevnik-uspekha /etc/nginx/sites-enabled/
  
  # Удалите дефолтную конфигурацию (если есть)
  sudo rm -f /etc/nginx/sites-enabled/default
  
  # Проверьте конфигурацию
  sudo nginx -t
  
  # Если всё ОК, перезапустите nginx
  sudo systemctl reload nginx
  ```

- [ ] **3.8. Проверьте статус nginx**
  ```bash
  sudo systemctl status nginx
  ```
  Должно быть: **active (running)**

---

### ШАГ 4: Проверка работы

- [ ] **4.1. Проверьте продакшн**
  - Откройте в браузере: http://89.104.74.123
  - Должна открыться страница приложения
  - Если не открывается, проверьте логи:
    ```bash
    sudo tail -f /var/log/nginx/dnevnik-uspekha-error.log
    ```

- [ ] **4.2. Проверьте тестовое окружение**
  - Откройте в браузере: http://89.104.74.123:8080
  - Должна открыться страница приложения
  - Если не открывается, проверьте:
    ```bash
    sudo netstat -tlnp | grep 8080
    # Должен быть процесс nginx на порту 8080
    ```

- [ ] **4.3. Проверьте права доступа**
  ```bash
  ls -la /var/www/dnevnik-uspekha
  ls -la /var/www/dnevnik-uspekha-test
  ```
  Файлы должны принадлежать вашему пользователю

---

### ШАГ 5: Тестирование автоматического деплоя

- [ ] **5.1. Тест деплоя в dev**
  ```bash
  # На локальном компьютере
  git checkout dev
  # Сделайте небольшое изменение (например, добавьте комментарий)
  git add .
  git commit -m "Test: проверка деплоя dev"
  git push origin dev
  ```
  - Перейдите: https://github.com/boofmebel/dnevnik-uspekha/actions
  - Должен запуститься workflow "Deploy to Test Server"
  - Дождитесь завершения (зелёная галочка ✅)
  - Проверьте: http://89.104.74.123:8080 - изменения должны появиться

- [ ] **5.2. Тест деплоя в main**
  ```bash
  # На локальном компьютере
  git checkout main
  git merge dev
  git push origin main
  ```
  - Перейдите: https://github.com/boofmebel/dnevnik-uspekha/actions
  - Должен запуститься workflow "Deploy to Server"
  - Дождитесь завершения (зелёная галочка ✅)
  - Проверьте: http://89.104.74.123 - изменения должны появиться

---

## 🚀 Рабочий процесс после настройки

### Разработка новой функции:

1. **Создайте ветку или переключитесь на dev:**
   ```bash
   git checkout dev
   ```

2. **Внесите изменения в код**

3. **Закоммитьте и запушьте:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin dev
   ```

4. **Проверьте на тестовом сервере:**
   - http://89.104.74.123:8080
   - Дождитесь завершения деплоя (1-2 минуты)

5. **Если всё ОК, мержите в main:**
   ```bash
   git checkout main
   git merge dev
   git push origin main
   ```

6. **Проверьте на продакшн:**
   - http://89.104.74.123
   - Дождитесь завершения деплоя (1-2 минуты)

---

## 🔧 Решение проблем

### Проблема: GitHub Actions не запускается

**Решение:**
- Проверьте, что все секреты добавлены правильно
- Проверьте, что workflow файлы находятся в `.github/workflows/`
- Проверьте логи в разделе Actions на GitHub

### Проблема: Деплой падает с ошибкой SSH

**Решение:**
```bash
# Проверьте подключение вручную:
ssh -i ~/.ssh/id_rsa root@89.104.74.123

# Проверьте, что ключ добавлен в authorized_keys на сервере:
cat ~/.ssh/authorized_keys
```

### Проблема: Nginx не запускается

**Решение:**
```bash
# Проверьте конфигурацию:
sudo nginx -t

# Посмотрите логи ошибок:
sudo tail -50 /var/log/nginx/error.log

# Проверьте, что порты не заняты:
sudo netstat -tlnp | grep -E '80|8080'
```

### Проблема: Страница не открывается

**Решение:**
```bash
# Проверьте, что nginx запущен:
sudo systemctl status nginx

# Проверьте, что файлы на месте:
ls -la /var/www/dnevnik-uspekha/index.html
ls -la /var/www/dnevnik-uspekha-test/index.html

# Проверьте логи доступа:
sudo tail -f /var/log/nginx/dnevnik-uspekha-access.log
```

### Проблема: Изменения не появляются после деплоя

**Решение:**
```bash
# На сервере проверьте, что код обновился:
cd /var/www/dnevnik-uspekha
git log -1
git status

# Очистите кеш браузера (Ctrl+Shift+R или Cmd+Shift+R)
```

---

## 📞 Быстрая проверка

Выполните эти команды для быстрой проверки:

```bash
# 1. Проверка SSH подключения
ssh root@89.104.74.123 "echo 'SSH работает!'"

# 2. Проверка nginx
ssh root@89.104.74.123 "sudo systemctl status nginx --no-pager | head -5"

# 3. Проверка файлов
ssh root@89.104.74.123 "ls -la /var/www/dnevnik-uspekha/index.html"

# 4. Проверка доступности
curl -I http://89.104.74.123
curl -I http://89.104.74.123:8080
```

---

## ✅ Финальная проверка

После выполнения всех шагов проверьте:

- [ ] Продакшн открывается: http://89.104.74.123
- [ ] Тест открывается: http://89.104.74.123:8080
- [ ] GitHub Actions запускаются при push
- [ ] Деплой завершается успешно
- [ ] Изменения появляются на сервере

---

## 📝 Итоговые ссылки

**Продакшн:** http://89.104.74.123  
**Тест:** http://89.104.74.123:8080

**GitHub Actions:** https://github.com/boofmebel/dnevnik-uspekha/actions

---

**Готово!** 🎉 Теперь у вас настроен автоматический деплой!

