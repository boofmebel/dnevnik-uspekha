# Настройка GitHub Secrets для автоматического деплоя

## 📋 Необходимые секреты

Для работы автоматического деплоя нужно настроить следующие секреты в GitHub:

### 1. Перейдите в настройки репозитория

1. Откройте репозиторий: https://github.com/boofmebel/dnevnik-uspekha
2. Перейдите в **Settings** → **Secrets and variables** → **Actions**
3. Нажмите **New repository secret**

### 2. Добавьте следующие секреты:

#### `SERVER_HOST`
- **Описание:** IP-адрес или домен вашего сервера
- **Пример:** `123.45.67.89` или `yourdomain.com`
- **Важно:** Без `http://` или `https://`

#### `SERVER_USER`
- **Описание:** Имя пользователя для SSH подключения
- **Пример:** `deploy` или `ubuntu` или `root`
- **Важно:** Пользователь должен иметь права на выполнение команд в директории проекта

#### `SERVER_SSH_KEY`
- **Описание:** Приватный SSH ключ для подключения к серверу
- **Как получить:**
  1. На вашем локальном компьютере выполните:
     ```bash
     ssh-keygen -t ed25519 -C "github-actions-deploy"
     ```
  2. Скопируйте содержимое приватного ключа (обычно `~/.ssh/id_ed25519`):
     ```bash
     cat ~/.ssh/id_ed25519
     ```
  3. Добавьте публичный ключ на сервер:
     ```bash
     ssh-copy-id -i ~/.ssh/id_ed25519.pub user@your-server.com
     ```
  4. Вставьте **приватный ключ** (всё содержимое файла) в секрет `SERVER_SSH_KEY`

#### `SERVER_PORT` (опционально)
- **Описание:** SSH порт сервера
- **По умолчанию:** `22`
- **Пример:** `2222` (если используется нестандартный порт)

#### `SERVER_PATH`
- **Описание:** Полный путь к директории проекта на сервере
- **Пример:** `/var/www/dnevnik-uspekha` или `/home/deploy/dnevnik-uspekha`
- **Важно:** Путь должен существовать на сервере

## 🔐 Безопасность

- ✅ Секреты хранятся в зашифрованном виде
- ✅ Секреты доступны только GitHub Actions
- ✅ Секреты не видны в логах (кроме случаев явного вывода)
- ❌ **НИКОГДА** не коммитьте секреты в код!

## 🧪 Проверка настройки

После добавления всех секретов:

1. Сделайте любой коммит в ветку `main`
2. Перейдите в **Actions** в репозитории
3. Должен запуститься workflow "Deploy to Production"
4. Проверьте логи выполнения

## 🚨 Устранение проблем

### Ошибка подключения по SSH
- Проверьте, что `SERVER_HOST` и `SERVER_PORT` правильные
- Убедитесь, что публичный ключ добавлен на сервер
- Проверьте права доступа к директории `SERVER_PATH`

### Ошибка выполнения команд
- Убедитесь, что пользователь `SERVER_USER` имеет права на выполнение команд
- Проверьте, что путь `SERVER_PATH` существует и доступен

### Ошибка git pull
- Убедитесь, что на сервере настроен git и есть доступ к репозиторию
- Проверьте, что директория `SERVER_PATH` является git репозиторием

## 📝 Пример настройки на сервере

```bash
# 1. Создайте директорию проекта
mkdir -p /var/www/dnevnik-uspekha
cd /var/www/dnevnik-uspekha

# 2. Клонируйте репозиторий (если ещё не клонирован)
git clone https://github.com/boofmebel/dnevnik-uspekha.git .

# 3. Настройте права доступа
chown -R deploy:deploy /var/www/dnevnik-uspekha

# 4. Убедитесь, что пользователь может выполнять git команды
sudo -u deploy git config --global --add safe.directory /var/www/dnevnik-uspekha
```

## 🔄 Ручной запуск деплоя

Если нужно запустить деплой вручную:

1. Перейдите в **Actions** → **Deploy to Production**
2. Нажмите **Run workflow**
3. Выберите ветку `main`
4. Нажмите **Run workflow**

