# CURSOR RULES — ДНЕВНИК УСПЕХА PROJECT (MAX VERSION)

**ВАЖНО:** Этот файл автоматически читается Cursor перед каждой задачей.  
Cursor должен строго соблюдать все указанные правила.

---

## 🎯 ОСНОВНЫЕ ПРИНЦИПЫ

1. **Никаких выдуманных функций, API, модулей, библиотек.**  
   - Использовать только реальные API FastAPI, SQLAlchemy, Angular, TypeScript и стандарты Web.  
   - Если не уверен — предложить 2–3 варианта решения с ссылками на официальные источники.

2. **Всегда выполнять самопроверку кода:**  
   - Синтаксис  
   - Корректность импортов и зависимостей  
   - Реальность функций/API  
   - Типизация (Python type hints / TypeScript strict)  
   - Edge cases  
   - Безопасность (XSS, CSRF, SQLi)  
   - Работоспособность и архитектурная целостность

3. **Production-ready подход**  
   - Код готов к продакшену или сопровождается чётким планом миграции  
   - ❌ Никаких временных костылей, «потом исправим» или недоделанных функций

4. **Anti-Hallucination Mode**  
   - Никаких придуманых API или функций  
   - Все нетривиальные решения — с указанием официальных источников или документации

---

## 🔒 BACKEND (FastAPI + SQLAlchemy)

### Архитектура и порядок

```
routers/ → thin controllers (только вызовы сервисов)
services/ → бизнес-логика
repositories/ → доступ к базе данных
schemas/ → Pydantic модели (request/response)
models/ → SQLAlchemy модели
core/ → конфиги, utils, exceptions, security
migrations/ → Alembic
```

- Все файлы должны быть на своих местах, без временных или инструкционных файлов.  
- Любые инструкции для пользователя писать **только в чате**, а не создавать документом.  

### Асинхронность
- Предпочтительно async-first: AsyncEngine + async_session  
- Синхронный код — только в BackgroundTasks или worker  
- Запрещено блокировать event loop

### JWT Security
- Access token: 5–15 минут жизни, хранится только в памяти  
- Refresh token: HttpOnly, Secure, SameSite cookie; ротация обязательна  
- ❌ Не хранить токены в localStorage  
- Refresh токены в БД или Redis: id, user_id, device_info, issued_at, revoked_at  
- `/auth/refresh` endpoint — rotation + новый access token  
- Логирование входов/выходов и подозрительных событий

### CSRF и cookie
- double-submit CSRF или Anti-CSRF header при использовании cookie  
- Flow документировать в чате  

### CORS
- ❌ Не использовать `*` в проде  
- Указывать только разрешённые origin'ы

### Rate limiting
- Ограничение на критичные endpoints: login, reset password  
- Хранение состояния в Redis или in-memory limiter

### Загрузка файлов
- Проверка MIME-type, расширений, magic bytes  
- Ограничение размера (например, 10MB)  
- Сканирование на вирусы  
- Хранение вне /static, отдача через Nginx/X-Accel-Redirect или CDN

### Миграции
- Только через Alembic  
- ❌ Не использовать `metadata.create_all` в проде  
- ❌ Не требовать sudo  

### Логи и мониторинг
- JSON-логи  
- Ошибки → Sentry  
- Метрики → Prometheus (опционально)

---

## 🎨 FRONTEND (Angular 16+)

### TypeScript / сборка
- strict: true  
- ❌ Не использовать any без крайней необходимости  
- ESLint + Prettier, production build (`ng build --configuration production`)

### Аутентификация
- Access token: память  
- Refresh token: HttpOnly cookie  
- HttpInterceptor:  
  - добавляет access token в Authorization header  
  - при 401 инициирует refresh и повтор запроса  
  - блокировка параллельных refresh'ей

### Архитектура клиента
- Lazy loading для /admin и мобильного модуля  
- Компоненты — UI-only  
- Бизнес-логика — сервисы  
- ReactiveForms для всех форм  
- ErrorInterceptor для обработки ошибок API  
- PWA: offline cache, SW обновления, versioning

### Безопасность фронтенда
- CSP и security headers на Nginx  
- Санитизация входных данных  
- ❌ Токены не в консоли

---

## 🗄️ DATABASE

- PostgreSQL для продакшена; SQLite только для dev/test  
- SQLAlchemy 2.0 async style preferred  
- Обработка IntegrityError, UniqueViolation  
- Бэкапы + тест восстановления  
- Индексы для полей поиска  
- ❌ Raw SQL без обоснования  

---

## 🚀 DEVOPS / NGINX / SYSTEMD

- HTTPS/TLS + HSTS  
- gzip/brotli  
- Cache-control: long-lived immutable для статических файлов, короткий для index.html  
- Systemd unit / Gunicorn + Uvicorn  
- Health-check endpoints: /health, /ready  
- Security headers: CSP, X-Frame-Options, X-Content-Type-Options, Referrer-Policy  
- Ограничение размера тела запроса  
- WAF / Rate-limit на edge (опционально)  
- Централизованные логи + ротация  
- ❌ Не требовать sudo  

---

## 📝 ПРОЦЕСС РАЗРАБОТКИ

1. PLANNING: архитектура, dependencies, миграции  
2. EXECUTION: реализация, unit/integration tests, CI checks  
3. VERIFICATION: линтеры, типизация, security scan, ручная проверка  

---

## ⚠️ ТЕКУЩИЕ КРИТИЧЕСКИЕ ПРОБЛЕМЫ

**Приоритет 1:**
1. Refresh token → HttpOnly cookie  
2. CSRF защита  
3. HTTPS/TLS + HSTS  
4. Rate limiting  
5. PostgreSQL вместо SQLite  

**Приоритет 2:**
6. Разделение на слои (routers/services/repositories)  
7. Миграции Alembic  
8. Структурированные логи  
9. CSP + остальные security headers  

---

## 🔧 ДОПОЛНИТЕЛЬНЫЕ ПРАВИЛА

- Самопроверка кода перед выдачей  
- Проверять существование файлов и зависимостей  
- ❌ Никаких TODO без уточнения  
- Все решения соответствуют стандартам senior-разработчика  
- Edge cases и ошибки обработаны  
- Любые изменения не ломают существующую архитектуру  
- Cursor выполняет все действия самостоятельно, **не предлагает sudo или команды пользователю**  
- Архитектура и папки проекта должны оставаться в порядке, без лишних и временных файлов  
- Любые инструкции для пользователя — **только в чате**, не создавать отдельные документы  

---

## ✅ ФИНАЛЬНАЯ ПРОВЕРКА

Перед выдачей решения Cursor обязан убедиться:

- [ ] Синтаксис и типизация корректны  
- [ ] Импорты и зависимости актуальны  
- [ ] Нет выдуманных API  
- [ ] Код production-ready  
- [ ] Edge cases обработаны  
- [ ] Соответствие security стандартам  
- [ ] Все изменения не ломают архитектуру  
- [ ] Порядок в папках и архитектуре сохранён, нет мусорных или временных файлов  

---

## 🚫 КРИТИЧЕСКОЕ ПРАВИЛО

**НИКОГДА НЕ УДАЛЯТЬ ФАЙЛ `cursor/rules_full.md`**

Этот файл является основой для работы Cursor и должен всегда присутствовать в проекте.

