# 📋 ПОШАГОВАЯ ИНСТРУКЦИЯ: УСТАНОВКА POSTGRESQL

## ШАГ 1: Установка Homebrew

### 1.1. Откройте Терминал
- Нажмите `Cmd + Space` (Spotlight)
- Введите "Терминал" или "Terminal"
- Нажмите Enter

### 1.2. Скопируйте и выполните команду установки Homebrew
```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### 1.3. Введите пароль администратора
- Система попросит ввести пароль вашего Mac
- Введите пароль (символы не будут отображаться - это нормально)
- Нажмите Enter

### 1.4. Дождитесь завершения установки
- Установка займет 5-10 минут
- В конце вы увидите сообщение об успешной установке
- Могут быть инструкции добавить Homebrew в PATH - выполните их

### 1.5. Добавьте Homebrew в PATH (если нужно)
Выполните команды, которые покажет установщик, обычно это:
```bash
echo 'eval "$(/opt/homebrew/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/opt/homebrew/bin/brew shellenv)"
```

Или для старых Mac (Intel):
```bash
echo 'eval "$(/usr/local/bin/brew shellenv)"' >> ~/.zprofile
eval "$(/usr/local/bin/brew shellenv)"
```

### 1.6. Проверьте установку
```bash
brew --version
```
Должна показаться версия Homebrew (например: `Homebrew 4.x.x`)

---

## ШАГ 2: Установка PostgreSQL через Homebrew

### 2.1. Установите PostgreSQL
```bash
brew install postgresql@14
```
Это займет несколько минут

### 2.2. Запустите PostgreSQL
```bash
brew services start postgresql@14
```

### 2.3. Проверьте, что PostgreSQL запущен
```bash
brew services list | grep postgresql
```
Должно показать `started` (зеленый)

---

## ШАГ 3: Создание базы данных

### 3.1. Создайте базу данных
```bash
createdb dnevnik_uspekha
```

Если возникнет ошибка "role postgres does not exist", создайте пользователя:
```bash
createuser -s postgres
createdb dnevnik_uspekha
```

---

## ШАГ 4: Проверка подключения

### 4.1. Проверьте подключение к БД
```bash
psql -d dnevnik_uspekha -c "SELECT version();"
```
Должна показаться версия PostgreSQL

---

## ШАГ 5: Выполнение миграции (я сделаю это автоматически)

После того, как вы выполните шаги 1-4, напишите мне, и я:
- ✅ Проверю подключение
- ✅ Выполню миграцию Alembic
- ✅ Проверю, что все работает

---

## 🆘 ЕСЛИ ЧТО-ТО ПОШЛО НЕ ТАК

### Проблема: "command not found: brew"
**Решение:** Добавьте Homebrew в PATH (см. шаг 1.5)

### Проблема: "Permission denied"
**Решение:** Убедитесь, что вы ввели правильный пароль администратора

### Проблема: "port 5432 is already in use"
**Решение:** PostgreSQL уже запущен или другой сервис использует порт
```bash
lsof -i :5432  # Проверить, что использует порт
```

### Проблема: "role postgres does not exist"
**Решение:** Создайте пользователя:
```bash
createuser -s postgres
```

---

## ✅ ЧЕКЛИСТ

- [ ] Homebrew установлен (`brew --version` работает)
- [ ] PostgreSQL установлен (`brew list | grep postgresql`)
- [ ] PostgreSQL запущен (`brew services list` показывает `started`)
- [ ] База данных создана (`psql -l | grep dnevnik_uspekha`)
- [ ] Подключение работает (`psql -d dnevnik_uspekha` открывает сессию)

---

**После выполнения всех шагов напишите мне - я выполню миграцию и проверю настройку!**

