# 📋 ИНФОРМАЦИЯ О ПРОЕКТЕ ДЛЯ НОВОГО АГЕНТА

## 🎯 ОПИСАНИЕ ПРОЕКТА

**Название:** Дневник успеха (Diary of Success)  
**Тип:** Веб-приложение для мотивации детей через систему виртуальных наград  
**Технологии:** FastAPI (backend), Vanilla JS (frontend), PostgreSQL  
**Сервер:** 89.104.74.123:3000 (frontend), :8000 (backend)  
**Статус:** В разработке, админ-панель не работает (401 ошибка)

---

## 📁 СТРУКТУРА ПРОЕКТА

```
/Users/evgeniypomytkin/Вика/
├── backend/                    # FastAPI backend
│   ├── core/                   # Основные модули
│   │   ├── config.py          # Конфигурация (SECRET_KEY, DATABASE_URL)
│   │   ├── database.py        # Подключение к БД
│   │   ├── dependencies.py    # FastAPI dependencies (get_current_user, check_admin_access)
│   │   └── security/
│   │       ├── jwt.py         # JWT токены (create_access_token, verify_token)
│   │       └── password.py    # Хеширование паролей
│   ├── models/                 # SQLAlchemy модели
│   │   ├── user.py            # User, UserRole (admin, parent, child)
│   │   ├── child.py           # Child
│   │   ├── subscription.py    # Subscription
│   │   ├── notification.py    # Notification
│   │   └── ...
│   ├── routers/                # API роутеры
│   │   ├── auth.py            # /api/auth/login, /register
│   │   ├── admin.py           # /api/admin/stats, /users, /children
│   │   └── ...
│   ├── services/               # Бизнес-логика
│   │   └── auth_service.py    # AuthService
│   ├── repositories/           # Доступ к БД
│   │   └── user_repository.py
│   └── main.py                # Точка входа FastAPI
│
├── frontend/                    # Frontend (Vanilla JS)
│   ├── admin.html              # Админ-панель
│   ├── static/css/
│   │   └── admin.css           # Стили админки + матричный эффект
│   └── src/js/
│       ├── api.js              # API клиент (apiClient)
│       ├── admin.js            # Логика админки
│       └── ...
│
└── docs/                       # Документация
```

---

## 🔐 ТЕКУЩАЯ ПРОБЛЕМА

### ❌ Проблема: 401 Unauthorized при доступе к админке

**Симптомы:**
- Токен передается в заголовке `Authorization: Bearer <token>` (длина 140 символов)
- Сервер возвращает 401 "Требуется авторизация"
- Пользователь найден в БД с ролью `"admin"` (строка)
- Прямой curl запрос с токеном работает!

**Логи:**
```
Token verification failed for token: eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
INFO: "GET /api/admin/stats HTTP/1.1" 401 Unauthorized
```

**Причина:**
HTTPBearer не получает токен из запроса браузера, хотя токен валиден.

---

## ✅ ЧТО УЖЕ СДЕЛАНО

1. ✅ Пользователь создан: `79059510009@mail.ru` / `Admin123!` (роль: admin)
2. ✅ Токен создается правильно (JWT, длина 140 символов)
3. ✅ Токен валиден и не истек
4. ✅ Роль преобразуется в строку в `get_current_user`
5. ✅ Добавлено логирование в `verify_token`
6. ✅ Время жизни токена увеличено до 60 минут
7. ✅ Добавлен fallback для получения токена из заголовка напрямую

---

## 🔧 КЛЮЧЕВЫЕ ФАЙЛЫ И КОД

### Backend: `backend/core/dependencies.py`

```python
security = HTTPBearer(auto_error=False)  # Отключена автоматическая ошибка

async def get_current_user(
    request: Request,  # Добавлен для прямого доступа к заголовкам
    credentials: Optional[HTTPAuthorizationCredentials] = Depends(security),
    db: AsyncSession = Depends(get_db)
) -> dict:
    # Пробуем получить токен из HTTPBearer
    token = None
    if credentials:
        token = credentials.credentials
    
    # Если не получили, пробуем из заголовка напрямую
    if not token:
        auth_header = request.headers.get("Authorization")
        if auth_header and auth_header.startswith("Bearer "):
            token = auth_header.replace("Bearer ", "").strip()
    
    # Проверка токена...
```

### Frontend: `frontend/src/js/api.js`

```javascript
// Токен передается в заголовке
config.headers['Authorization'] = `Bearer ${token}`;
```

### Frontend: `frontend/src/js/admin.js`

```javascript
// Токен сохраняется в localStorage
localStorage.setItem('admin_token', token);
apiClient.setAccessToken(token);
```

---

## 🗄️ БАЗА ДАННЫХ

**Подключение:** PostgreSQL  
**URL:** Из переменной окружения `DATABASE_URL`  
**Пользователь:** `79059510009@mail.ru` (ID: 1, роль: "admin")

**Проверка пользователя:**
```sql
SELECT id, email, role FROM users WHERE email = '79059510009@mail.ru';
-- Результат: id=1, email='79059510009@mail.ru', role='admin'
```

---

## 🚀 КОМАНДЫ ДЛЯ РАБОТЫ

### Подключение к серверу
```bash
ssh root@89.104.74.123
```

### Перезапуск бэкенда
```bash
cd /var/www/dnevnik-uspekha/backend
source venv/bin/activate
pkill -f 'uvicorn.*8000'
nohup python3 -m uvicorn main:app --host 0.0.0.0 --port 8000 > /tmp/backend.log 2>&1 &
```

### Просмотр логов
```bash
tail -50 /tmp/backend.log
```

### Копирование файлов на сервер
```bash
scp backend/core/dependencies.py root@89.104.74.123:/var/www/dnevnik-uspekha/backend/core/
```

### Тестирование токена
```bash
# Создать токен
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"79059510009@mail.ru","password":"Admin123!"}'

# Использовать токен
curl -X GET http://localhost:8000/api/admin/stats \
  -H "Authorization: Bearer <token>"
```

---

## 🔍 ДИАГНОСТИКА

### Проверка что токен валиден
```python
from core.security.jwt import verify_token
payload = verify_token(token)
print(payload)  # Должен вернуть {'sub': '1', 'exp': ..., 'type': 'access'}
```

### Проверка что пользователь найден
```python
from repositories.user_repository import UserRepository
user = await user_repo.get_by_id(1)
print(user.role)  # Должно быть "admin"
```

### Проверка что роль проверяется правильно
```python
user_role = "admin"
is_admin = user_role == "admin" or user_role == UserRole.ADMIN.value
print(is_admin)  # Должно быть True
```

---

## 💡 ВАРИАНТЫ РЕШЕНИЯ ПРОБЛЕМЫ

### Вариант 1: Исправить HTTPBearer (РЕКОМЕНДУЕТСЯ)
- Проверить что заголовок `Authorization` правильно передается
- Убедиться что нет проблем с CORS
- Проверить что `HTTPBearer` правильно настроен

### Вариант 2: Использовать только Request (ТЕКУЩЕЕ РЕШЕНИЕ)
- Получать токен напрямую из `request.headers.get("Authorization")`
- Убрать зависимость от HTTPBearer

### Вариант 3: Добавить отладочное логирование
- Логировать все заголовки запроса
- Логировать что получает HTTPBearer
- Логировать что получает Request

---

## 📝 ВАЖНЫЕ ЗАМЕТКИ

1. **Токен истекает через 60 минут** (было 15, увеличено для отладки)
2. **Роль хранится как строка** в БД, но может быть Enum в коде
3. **Матричный эффект** добавлен на страницу входа админки
4. **Токен сохраняется в localStorage** как `admin_token`
5. **Бэкенд работает на порту 8000**, фронтенд на 3000
6. **Nginx проксирует** запросы с 3000 на 8000

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Исправить получение токена (добавлен fallback)
2. ⏳ Протестировать что токен правильно получается из Request
3. ⏳ Убедиться что админка загружает данные
4. ⏳ Убрать временное логирование после исправления
5. ⏳ Вернуть время жизни токена к 15 минутам

---

## 📞 КОНТАКТЫ И РЕСУРСЫ

- **Сервер:** 89.104.74.123
- **Frontend:** http://89.104.74.123:3000/admin.html
- **Backend API:** http://89.104.74.123:3000/api/
- **Логи:** /tmp/backend.log на сервере
- **Код:** /var/www/dnevnik-uspekha/ на сервере

---

## 🔑 УЧЕТНЫЕ ДАННЫЕ

**Админ:**
- Email: `79059510009@mail.ru`
- Password: `Admin123!`
- Role: `admin`

**База данных:**
- Проверка через SQL: `SELECT * FROM users WHERE email = '79059510009@mail.ru';`

---

**Последнее обновление:** 2025-12-18  
**Статус:** Проблема с авторизацией, исправления применены, требуется тестирование

